# Developer Brand Site — 视觉设计系统

> 状态：阶段二已定义  
> 范围：视觉语言、设计令牌、组件契约、响应式与动效规范  
> 非范围：本阶段不创建 Next.js 项目、不安装依赖、不编写页面、不建立数据库、不接入 Three.js 或生成正式图片资产。

---

## 0. 使用方式与边界

这是后续所有界面实现的唯一视觉基准。阶段三开始前，新的页面、组件、动效和内容模板都应先遵守本文件；需要偏离时，先记录原因和替代规则，不能临时在组件里写一套新风格。

本网站不是“炫技开发者模板”，也不是黑底霓虹游戏站。它是一个让通过抖音、小红书、搜索或转介绍来到网站的客户，在很短时间内感到可靠、能落地、懂产品且有审美的个人品牌入口。

### 0.1 品牌句

**用可靠的软件工程，把业务想法梳理成清晰、可用、可持续的数字产品。**

### 0.2 设计人格

- 冷静：信息优先，留白充足，不用无意义的高频动效。
- 精密：像一套被校准过的工具，而非“科技感素材”的拼贴。
- 有方向感：紫色只承担关键行动与系统主线；青色只提示流动、连接和状态。
- 有温度：文案与案例图像承担信任，不用冰冷的技术符号取代人和业务。

### 0.3 一句话视觉场景

夜间在手机上从短视频进入的创业者，应像打开一间被整理好的数字工坊：黑色空间安静、信息层级清楚，右侧有一束缓慢编织的数据光线，告诉他“这个人能把复杂事做明白”。

### 0.4 明确不做

- 不复制 Hubtown、Apple、Linear、Vercel、Framer 或任何参考站的版式、素材、文案、Logo 或独特图形。
- 不使用旋转地球、满屏随机粒子、赛博网格、彩虹渐变标题、玻璃卡片瀑布流作为默认视觉。
- 不让 Hover 成为获得关键信息的唯一方式。
- 不在移动端保留桌面端的横向滚动、固定叙事或鼠标特效。
- 不以加载动画、全屏遮罩或滚动劫持阻碍访问内容。

---

## 1. 研究记录与方向决策

### 1.1 参考研究结论

本阶段研究了深色开发者作品集、创意案例页面、专业服务联系表单与移动端全屏菜单的公开界面，只提取可迁移原则，不复用任何视觉资产：

| 观察对象         | 保留的原则                                            | 明确舍弃                                   |
| ---------------- | ----------------------------------------------------- | ------------------------------------------ |
| 深色开发者作品集 | 首屏主张与 CTA 必须清晰；深色层级要靠明度而非满屏阴影 | 蓝紫渐变大标题、重复徽章、模板式技术标签墙 |
| 创意案例展示     | 案例以大画面和叙事顺序建立信任                        | 为了“酷”而让项目卡片难以扫描               |
| 服务咨询表单     | 大标题引导情绪，单列字段保持克制与可填写性            | 复杂分步表单、装饰性图标淹没字段           |
| 移动端全屏菜单   | 大触控目标、高对比、明确关闭与焦点行为                | 把桌面导航简单缩小或依赖悬停               |

### 1.2 两个视觉概念

| 概念                               | 描述                                                                                 | 决定                                             |
| ---------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------ |
| **A. System Loom（系统织机）**     | 半透明石墨模块之间有低亮度紫青“数据丝线”缓慢重组；左侧留给明确文案，右侧承载视觉张力 | **采用**：作为 Hero 和少数案例转场的专属识别元素 |
| B. Engineering Archive（工程档案） | 单块被刻蚀的深色技术面板，像硬件或工业控制台                                         | 不采用：容易向游戏化、硬件化和封闭感偏移         |

System Loom 不是正式图片资产，也不是每个区块都要出现的背景。后续实施时，它可以由轻量 R3F 场景、SVG/CSS 静态构图或普通海报图降级实现；无论哪种实现，都必须保留可读的文本安全区。

### 1.3 参考语言的取舍

- 采用深色原生界面的明度分层、细边界、克制紫色和内容优先的节奏。
- 采用品牌站“一个折叠面只表达一个核心想法”的叙事，而非每屏堆很多卡片。
- 不采用编辑杂志式衬线、大量等宽字体标签、反复出现的小型全大写 kicker。

---

## 2. 视觉系统总览

### 2.1 设计原则

1. **先读懂，再惊艳。** 任何装饰不应降低标题、案例、服务和联系入口的清晰度。
2. **一个页面只有一个主动作。** 主色按钮每个视口最多一个；次动作用描边、文字链接或普通按钮。
3. **用明度建层，不靠厚阴影。** 黑色背景中的层级依次来自背景、表面、边界、文字和少量色彩。
4. **色彩有职责。** 紫色是品牌动作；青色是流动/辅助；状态色只能表达状态。
5. **动效是提示，不是内容。** 所有内容在无动效时也完整、可读、可操作。

### 2.2 令牌命名规则

- 基础令牌以 --ref- 命名，仅供系统内部校准。
- 语义令牌以 --color-、--space-、--radius-、--motion- 命名，组件只能使用语义令牌。
- 不在业务组件中直接书写 Hex、任意阴影、任意圆角和任意动画时长。
- 颜色以 OKLCH 作为后续 CSS 源值，以 Hex 作为设计审阅参考值。

---

## 3. 色彩与表面

### 3.1 深色主题是唯一首发主题

首发只建设深色主题，不在阶段三顺便塞入浅色模式。若后续需要浅色主题，应重新做一轮对比度、图像和动效审阅，不能简单反转颜色。

### 3.2 基础与语义色

| 语义令牌                     | OKLCH 源值               | Hex 参考     | 使用范围                     |
| ---------------------------- | ------------------------ | ------------ | ---------------------------- |
| --color-canvas               | oklch(0.115 0 0)         | #050505      | 页面底色、沉浸式黑场         |
| --color-canvas-subtle        | oklch(0.146 0.004 285.9) | #0A0A0C      | 区块底层、导航滚动后背景     |
| --color-surface-1            | oklch(0.179 0.006 285.8) | #111114      | 主要浮层、输入框、菜单       |
| --color-surface-2            | oklch(0.215 0.008 285)   | #1A1A1F      | 提升态、选中态背景           |
| --color-surface-hover        | oklch(0.245 0.010 285)   | 近似 #222228 | 可悬停的表面                 |
| --color-text-primary         | oklch(0.976 0.001 286.4) | #F7F7F8      | 主标题、正文、按钮反白文字   |
| --color-text-secondary       | oklch(0.772 0.011 286.1) | #B4B4BC      | 说明、辅助正文               |
| --color-text-tertiary        | oklch(0.593 0.015 285.9) | #7D7D87      | 元信息、非核心标签           |
| --color-text-disabled        | oklch(0.452 0.013 285.9) | #55555D      | 禁用状态；不能用作正常正文   |
| --color-text-inverse         | oklch(0.134 0 0)         | #080808      | 浅色状态块、青色按钮内文字   |
| --color-brand-reference      | oklch(0.597 0.224 279.8) | #6C63FF      | 品牌光晕、焦点、非文字色块   |
| --color-action-primary       | oklch(0.555 0.220 280)   | 近似 #564DDC | 主按钮填充，保证浅色文字可读 |
| --color-action-primary-hover | oklch(0.575 0.220 280)   | 近似 #5B53E6 | 主按钮悬停                   |
| --color-accent-cyan          | oklch(0.797 0.134 211.5) | #22D3EE      | 数据流、图表、少量辅助高光   |
| --color-accent-violet        | oklch(0.627 0.233 303.9) | #A855F7      | 极少量辅助高光，非正文色     |
| --color-success              | oklch(0.723 0.192 149.6) | #22C55E      | 成功、可用、在线             |
| --color-warning              | oklch(0.769 0.165 70.1)  | #F59E0B      | 等待、注意                   |
| --color-danger               | oklch(0.637 0.208 25.3)  | #EF4444      | 错误、危险操作               |
| --color-info                 | oklch(0.623 0.188 259.8) | #3B82F6      | 中性信息                     |

### 3.3 透明表面、边界与环境光

| 令牌                         | 值                                     | 规则                                         |
| ---------------------------- | -------------------------------------- | -------------------------------------------- |
| --color-surface-raised       | rgb(255 255 255 / 0.04)                | 仅用于深色表面的轻提升                       |
| --color-surface-raised-hover | rgb(255 255 255 / 0.07)                | 卡片/菜单 Hover                              |
| --color-border-subtle        | rgb(255 255 255 / 0.08)                | 默认分隔与容器边界                           |
| --color-border-default       | rgb(255 255 255 / 0.12)                | 可交互控件和卡片                             |
| --color-border-strong        | rgb(255 255 255 / 0.22)                | 选中、拖拽、强调状态                         |
| --color-overlay              | rgb(0 0 0 / 0.72)                      | 菜单、媒体、弹层遮罩                         |
| --shadow-focus               | 0 0 0 2px canvas + 0 0 0 4px brand     | 唯一焦点环样式                               |
| --ambient-system             | 紫色 16% 与青色 10% 的大尺度径向环境光 | 只用于 Hero/专题背景，不能用于文字或普通卡片 |

禁止创建“渐变标题”或“渐变卡片”令牌。渐变只能作为低不透明度环境光、图像遮罩或 System Loom 的光线，不作为文字填充。

### 3.4 对比度结论

| 前景 / 背景              | 约束           | 结果与处理                               |
| ------------------------ | -------------- | ---------------------------------------- |
| #F7F7F8 / #050505        | 主文本         | 约 19.0:1，可用                          |
| #B4B4BC / #050505        | 辅助正文       | 约 9.9:1，可用                           |
| #7D7D87 / #050505        | 元信息         | 约 5.0:1，可用                           |
| #55555D / #050505        | 正常文本       | 约 2.8:1，不可用；只限禁用/装饰          |
| #F7F7F8 / #6C63FF        | 白字品牌按钮   | 约 4.0:1，不可用；因此不用原紫做白字按钮 |
| #F7F7F8 / action primary | 主按钮         | 目标不低于 4.5:1                         |
| #080808 / #22D3EE        | 青色填充内文字 | 高对比，可用                             |

状态不能只靠颜色表达：成功、错误、提示必须同时配合图标、文本或清晰的状态标签。

---

## 4. 字体与排版

### 4.1 字体角色

| 角色              | 字体栈                                                        | 原因                                 | 禁止用途                         |
| ----------------- | ------------------------------------------------------------- | ------------------------------------ | -------------------------------- |
| 展示标题          | Onest, Noto Sans SC, PingFang SC, Microsoft YaHei, sans-serif | 轮廓紧凑、稳定，适合“被校准的工具”感 | 不做超细字重或倾斜装饰           |
| 正文与 UI         | Geist, Noto Sans SC, PingFang SC, Microsoft YaHei, sans-serif | 阅读平衡、界面信息清晰               | 不把它当大面积装饰海报字         |
| 代码/真实技术片段 | Geist Mono, SFMono-Regular, Consolas, monospace               | 只用于代码、命令和数据值             | 不用作所有栏目小标题或技术感装饰 |

不使用 Inter、Space Grotesk、IBM Plex 或“全站等宽字体”作为默认品牌捷径。中文优先使用系统可用的现代无衬线回退；后续如加载 Noto Sans SC，应做子集与字体显示优化。

### 4.2 字号层级

| Token      | 桌面范围                        | 手机基准 | 字重 | 行高 | 典型用途                   |
| ---------- | ------------------------------- | -------- | ---- | ---- | -------------------------- |
| display-xl | clamp(54px, 5.7vw + 16px, 96px) | 54px     | 600  | 0.96 | 单个首屏核心主张           |
| display-lg | clamp(46px, 4vw + 16px, 80px)   | 46px     | 600  | 0.98 | 大章节标题、Footer 主句    |
| display-md | clamp(40px, 3vw + 16px, 64px)   | 40px     | 600  | 1.00 | 章节主标题                 |
| h1         | clamp(36px, 2.4vw + 14px, 52px) | 36px     | 600  | 1.08 | 一级页面标题               |
| h2         | clamp(30px, 1.8vw + 14px, 40px) | 30px     | 600  | 1.15 | 二级标题                   |
| h3         | clamp(24px, 1.3vw + 14px, 30px) | 24px     | 600  | 1.22 | 卡片/小节标题              |
| h4         | clamp(20px, 1vw + 13px, 24px)   | 20px     | 600  | 1.30 | 表单组、内容小标题         |
| body-lg    | 20px                            | 18px     | 400  | 1.70 | 引言、关键说明             |
| body-md    | 16px                            | 16px     | 400  | 1.70 | 正文、卡片描述             |
| body-sm    | 14px                            | 14px     | 450  | 1.60 | 次要内容                   |
| caption    | 12px                            | 12px     | 500  | 1.50 | 日期、辅助元信息           |
| utility    | 12px                            | 12px     | 600  | 1.40 | 极少量短标签；不得铺满页面 |

### 4.3 排版细则

- 英文展示标题字距可在 -0.035em 至 -0.015em；中文标题最多 -0.01em，避免字形挤压。
- 光色文字在黑底上比同级深色文字多留 0.05–0.10 的行高；正文不小于 1.6。
- 英文 Hero 标题最多 2–3 行；中文最多 3 行，按语义手动换行，不用缩小字体强行塞下。
- 长正文最大行宽：中文 30–36 个全角字符、英文 60–72 个字符；使用独立 TextContainer，不与全宽媒体共享宽度。
- 标题下的说明文字优先使用 text secondary，不使用低对比灰色。
- 不连续重复“全部大写 + 加宽字距”的栏目标签；若需要栏目定位，只在叙事起点使用一次短标签。

---

## 5. 尺寸、网格、圆角与层级

### 5.1 间距系统

基础单位为 4px。后续不得出现 13px、37px 这类无令牌间距，除非它是图标视觉校正并有注释。

| Token   |   值 | Token    |    值 |
| ------- | ---: | -------- | ----: |
| space-1 |  4px | space-8  |  32px |
| space-2 |  8px | space-10 |  40px |
| space-3 | 12px | space-12 |  48px |
| space-4 | 16px | space-16 |  64px |
| space-5 | 20px | space-20 |  80px |
| space-6 | 24px | space-24 |  96px |
| space-7 | 28px | space-32 | 128px |
|         |      | space-40 | 160px |

区块纵向节奏使用 clamp(72px, 10vw, 160px)；紧凑区块可使用 clamp(56px, 7vw, 104px)。同一页面必须有“密”和“疏”的节奏，不能每段都使用同一个大间距。

### 5.2 容器与网格

| 视口          | 内容最大宽度 | 列数 | 栏间距 | 两侧安全边距 |
| ------------- | -----------: | ---: | -----: | -----------: |
| 1440px 及以上 |       1280px |   12 |   24px |      48–72px |
| 1280–1439px   |       1120px |   12 |   24px |         40px |
| 1024–1279px   |        960px |   12 |   20px |         32px |
| 768–1023px    |         100% |    8 |   20px |      28–32px |
| 375–767px     |         100% |    4 |   16px |         20px |

容器角色：

- PageContainer：全宽背景和页面级定位。
- ContentContainer：最大 1280px 的通用内容。
- WideContainer：最大 1440px，仅供沉浸式媒体/大案例。
- TextContainer：最大 720px，正文和说明。
- NarrowContainer：最大 560px，联系表单、确认状态。

### 5.3 圆角、边界、阴影

| Token       |    值 | 用途                 |
| ----------- | ----: | -------------------- |
| radius-xs   |   6px | 轻量标签、内部小控件 |
| radius-sm   |  10px | 输入框、普通按钮     |
| radius-md   |  14px | 常规卡片、媒体容器   |
| radius-lg   |  20px | 大媒体、移动端菜单   |
| radius-pill | 999px | 状态点、极少数短标签 |

- 卡片默认用 14px，媒体用 20px；禁止以 24px+ 的过圆卡片制造“高级感”。
- 默认不加投影；需要浮层时用一层轻阴影：0 18px 60px rgb(0 0 0 / 0.28)。
- 不同时叠加“明显边框 + 大柔阴影 + 高透明玻璃模糊”。三者只能择一或两项，且必须有层级理由。
- 磨砂玻璃仅用于滚动后的导航、全屏菜单内层、浮动媒体控制；普通内容卡片不用玻璃。

### 5.4 层级

| Token     | 层级 | 用途             |
| --------- | ---: | ---------------- |
| z-base    |    0 | 正常内容         |
| z-raised  |   10 | 局部悬浮组件     |
| z-sticky  |   20 | Sticky 区块      |
| z-nav     |   40 | 导航             |
| z-popover |   60 | 下拉、Toast      |
| z-modal   |   80 | Dialog、媒体灯箱 |
| z-toast   |  100 | 全局通知         |

---

## 6. 图像、图形与信息密度

### 6.1 图像角色

后续项目案例、文章和服务页都必须使用真实的项目截图、经授权的摄影图、可验证的产品图或自制的图形化说明。黑色占位块不能替代案例图像。

| 类型          | 推荐比例   | 行为                                         |
| ------------- | ---------- | -------------------------------------------- |
| 项目主视觉    | 16:10      | 大画面，允许轻微遮罩，标题不能压在复杂信息上 |
| 博客封面      | 16:9       | 保持统一裁切与 alt 文案                      |
| 服务/过程配图 | 4:3 或 3:2 | 辅助理解，不做无意义素材墙                   |
| 头像          | 1:1        | 清晰、自然、低修图                           |
| 手机产品截图  | 9:19.5     | 放在适度留白内，不做多层设备框               |

### 6.2 System Loom 规范

- 画面由 3–6 个低对比度半透明平面、1–3 条紫青色数据线和稀疏节点构成。
- 80% 视觉重量在右侧/下侧，左侧保留文本安全区；RTL 或窄屏时不强行镜像而是将视觉移到标题之后。
- 线条亮度很低，不能与 CTA、焦点环、状态色争夺注意力。
- 桌面可缓慢漂移；平板减少节点和运动；手机改为静态海报或无动画 SVG。
- 不显示“全球网络”“代码雨”“粒子爆炸”或无限网格。

### 6.3 信息密度

- 一个首屏只允许一个视觉主角、一个标题组、一个主 CTA 和至多一个次 CTA。
- 常规内容区域每行不超过 3 张信息卡；卡片不是默认布局单位。
- 当信息需要比较时优先使用明确的表格、列表或段落，不用四张相似卡片硬拆。
- 数据展示必须有标签、单位、更新时间或语境，不能只放大数字。

---

## 7. 核心组件契约

以下是组件实现前必须满足的接口与状态约束。组件名是未来目录建议，不代表本阶段已创建任何文件。

### 7.1 布局与文本

| 组件             | 职责               | 关键变体/参数                                            | 响应式与无障碍要求                            |
| ---------------- | ------------------ | -------------------------------------------------------- | --------------------------------------------- |
| PageContainer    | 页面背景、基础层级 | tone: canvas / subtle                                    | 不承载可交互语义                              |
| ContentContainer | 受控内容宽度       | default / wide / narrow / text                           | 使用网格安全边距，不能靠固定宽度溢出          |
| Section          | 统一纵向节奏与锚点 | spacing: compact / default / spacious                    | 所有锚点要考虑固定导航偏移                    |
| Stack / Cluster  | 垂直/水平内容间距  | gap 只接收间距令牌                                       | 小屏自动换行；不可用负 margin 拼布局          |
| Divider          | 轻量分隔           | subtle / default                                         | 不能替代标题层级                              |
| DisplayHeading   | 大型叙事标题       | size: xl / lg / md；tone: default / muted / accent-solid | 不提供 GradientText；标题必须是语义化 h1–h3   |
| SectionHeading   | 区块标题组         | align: start / center                                    | 最多一处居中标题连续出现，避免模板感          |
| Paragraph        | 正文               | size: lg / md / sm                                       | 有可控行宽和中英文换行                        |
| MonoValue        | 代码/数据值        | copyable 可选                                            | 不作栏目装饰；可复制时提供键盘/屏幕阅读器提示 |

**冲突处理：** 初始提示词中的 GradientText 组件不进入设计系统。它会降低深色界面中的文本对比度并迅速形成模板感；需要强调时使用纯色 accent-solid、尺寸、留白或单独的环境光。

### 7.2 按钮、链接与导航交互

| 组件           | 变体                            | 尺寸                          | 必须状态                                                        |
| -------------- | ------------------------------- | ----------------------------- | --------------------------------------------------------------- |
| Button         | primary、secondary、ghost、text | sm 36px、md 44px、lg 52px     | default、hover、active、focus-visible、disabled、loading        |
| IconButton     | ghost、solid                    | 最小 40px；触摸端 44px        | 同 Button，必须有可访问名称                                     |
| ArrowLink      | inline / block                  | 跟随文字字号                  | hover、focus-visible、external                                  |
| MagneticButton | 仅 primary CTA                  | 52px 或以上                   | 仅 hover + fine pointer；其他设备退化为普通 Button              |
| Navigation     | desktop / mobile                | 80px 桌面高；64–72px 移动端高 | transparent、scrolled、hidden-on-down、visible-on-up、menu-open |

#### 按钮规则

- Primary：action primary 填充、主文字颜色；每个主要视口最多一个。
- Secondary：透明或 raised 表面 + 默认边界；不与 Primary 争夺视觉重量。
- Ghost：无常驻背景；Hover 只提升表面明度。
- Text：不做伪按钮背景；需要有清晰可辨的下划线或箭头位移。
- Hover 最多上移 2px 或箭头右移 3px，不能同时大幅缩放、发光、旋转。
- Active 使用轻微压下或 scale(0.98)；Loading 保留按钮宽度，不能让布局跳动。
- focus-visible 必须使用统一双层焦点环，不能只靠颜色变化。

#### 导航规则

- 顶部初始透明；滚动跨过首屏安全区域后使用低透明深色背景和轻模糊。
- 向下滚动隐藏、向上滚动出现的阈值应有 12–24px 容差，避免抖动。
- 移动端菜单是全屏/近全屏层，打开后锁定背景滚动，首个可聚焦元素为关闭按钮；支持 Esc、Tab 焦点循环和关闭后焦点回归。
- 锚点跳转后，目标内容不得被导航遮盖；不要劫持原生键盘或浏览器导航。

### 7.3 内容、媒体与数据组件

| 组件            | 变体                                                             | 基本规则                                                |
| --------------- | ---------------------------------------------------------------- | ------------------------------------------------------- |
| ContentCard     | project、blog、service、skill、process、testimonial、oss、metric | 先定义内容密度，再决定是否用卡片；默认无大阴影          |
| ProjectCard     | editorial-large、compact                                         | 首选大尺寸编辑式卡片；媒体、结果、技术与 CTA 可同时扫读 |
| BlogCard        | featured、standard                                               | 封面、分类、阅读时长和标题；不能让标签超过标题权重      |
| ServiceCard     | featured、plain                                                  | 讲适用情境、交付和行动入口，而非单列技术名词            |
| SkillCluster    | grouped、expanded                                                | 按能力域分组；技术图标不是装饰墙                        |
| Metric          | inline、featured                                                 | 数字必须带语境、单位或说明                              |
| Tag / Badge     | neutral、brand、status                                           | 短文本；状态色不等于分类色                              |
| ResponsiveImage | cover、contain                                                   | 固定比例、明确尺寸、真实 alt                            |
| VideoPlayer     | inline、lightbox                                                 | 自主播放仅静音；控件可键盘操作；无障碍提供封面/文字说明 |
| ImageReveal     | soft、mask                                                       | 只用于少量叙事媒体，静态状态必须完整可见                |

#### 卡片交互规则

- 默认：边界低对比、背景平静、内容优先。
- Hover：只选一种主反应——边界增强、媒体放大、上移 2px 或图标位移，不能同时全开。
- ProjectCard 在桌面精细指针设备可选轻倾斜，最大 4°；触摸、键盘和 reduced motion 均关闭倾斜。
- 卡片若整体可点击，内部不得再嵌套模糊的可点击元素；需要第二操作时提供清楚的独立按钮。

### 7.4 表单与反馈组件

| 组件                                 | 规格                          | 状态和行为                                              |
| ------------------------------------ | ----------------------------- | ------------------------------------------------------- |
| FormField                            | label 在上、帮助/错误文本在下 | label 不可只用 placeholder 替代                         |
| Input / Select                       | 高 48px；移动端文字至少 16px  | default、hover、focus、filled、disabled、error、success |
| Textarea                             | 最小高 144px                  | 显示剩余字符仅在有限制时使用                            |
| Checkbox / Radio                     | 触控目标至少 44px             | 键盘可操作，选中状态不只靠颜色                          |
| BudgetSelector / ProjectTypeSelector | 标准选择控件的组合形式        | 不把业务问题做成花哨卡片谜题                            |
| FormMessage                          | error、help、success          | 与字段的 aria-describedby 关联                          |
| LoadingIndicator                     | inline、section               | 不遮挡已可展示内容；有文字或无障碍名称                  |
| Toast                                | success、error、info          | aria-live 合理播报；不打断输入焦点                      |
| EmptyState / ErrorState              | inline、section               | 有明确下一步，而非只有抽象插画                          |

表单 Focus 不能只把边框从灰变紫；必须同时显示焦点环。错误提示紧跟字段并说明如何修复，不能只写“输入错误”。

### 7.5 动效基础组件

| 组件                 | 允许用途                                    | 限制                                             |
| -------------------- | ------------------------------------------- | ------------------------------------------------ |
| MotionProvider       | 集中处理 reduced motion、设备能力、全局切换 | 不能让业务组件各自猜测设备能力                   |
| Reveal               | 区块、卡片、媒体进入                        | 默认内容先可见；最多 opacity + 12–20px 位移      |
| Stagger              | 同组不超过 6 项的内容                       | 间隔 40–80ms，不做长队列                         |
| TextReveal           | Hero/大章节的按行揭示                       | 中文按行，不逐字；屏幕阅读器只有一份完整文本     |
| ParallaxLayer        | 装饰媒体或 System Loom                      | 位移不超过 12px；仅精细指针/非 reduced motion    |
| HoverTilt            | 少量项目媒体                                | 最大 4°，小屏禁用                                |
| PageTransition       | 路由之间的柔和衔接                          | 不遮罩内容超过 800ms                             |
| SmoothScrollProvider | 优化滚动手感                                | 不修改锚点、键盘、选择文本和 reduced motion 行为 |

---

## 8. 组件状态矩阵

| 组件 | Default | Hover | Focus-visible | Active | Disabled / Loading | 移动端 / Reduced Motion |
| --- | --- | --- | --- | --- | --- |
| 主按钮 | 紫色填充、白字 | 仅明度提升与最多 2px 位移 | 统一双层焦点环 | scale(.98) | 降低不透明度、禁止点击、保留宽度 | 无 hover；点击反馈 120ms；无磁吸 |
| 次按钮 | 透明/轻表面、边界 | 表面明度上升 | 焦点环 | 轻压下 | 可读但不可点 | 同上 |
| 文本链接 | 正常正文色 + 箭头/下划线 | 箭头 3px 位移或下划线展开 | 焦点环/明确下划线 | 无大缩放 | 不可操作时不用伪链接 | 保留下划线，不要求 hover |
| 卡片 | 低对比边界、媒体静态 | 只触发一种轻反应 | 全卡可聚焦且有焦点环 | 无 | 非可点击卡片不接收焦点 | 取消倾斜和仅 hover 信息 |
| 输入框 | Surface 1 + 默认边界 | 边界增强 | 品牌焦点环 + 边界增强 | 无 | 对比仍足够；loading 不可编辑 | 字号至少 16px，避免 iOS 自动放大 |
| 导航 | 透明或滚动表面 | 链接状态清晰 | 键盘可见 | 无 | 无 | 菜单大目标、Esc 关闭、焦点陷阱 |
| 视频/媒体 | 海报/首帧 | 控件显现可选 | 控件键盘可达 | 播放/暂停清楚 | 失败提供文字与替代动作 | 不自动播放有声/无穷运动 |
| Toast | 静态可读 | 无需 hover | 可操作按钮可聚焦 | 无 | 定时消失前不遮挡操作 | reduced motion 使用即时出现/消失 |

---

## 9. 响应式设计规范

### 9.1 断点

| 名称 | 范围      | 布局决策                               |
| ---- | --------- | -------------------------------------- |
| xs   | 375px 起  | 最小可用手机基准                       |
| sm   | 640px 起  | 大手机/小横屏                          |
| md   | 768px 起  | 平板，8 列开始                         |
| lg   | 1024px 起 | 笔记本，允许双栏与有限 sticky          |
| xl   | 1280px 起 | 12 列标准桌面                          |
| 2xl  | 1440px 起 | 宽屏，只扩大留白与媒体，不无限拉宽正文 |

### 9.2 逐档行为

| 场景     | 1440+ 桌面              | 1024–1279 笔记本 | 768–1023 平板      | 375–767 手机                      |
| -------- | ----------------------- | ---------------- | ------------------ | --------------------------------- |
| 导航     | 完整链接 + CTA          | 完整/精简链接    | 菜单优先           | 全屏菜单                          |
| 首屏     | 文案与 System Loom 双栏 | 双栏压缩         | 文字在上、视觉在下 | 文字优先；System Loom 静态或省略  |
| 内容网格 | 12 列，可不对称         | 12 列，减少跨度  | 8 列、二栏为主     | 4 列、单栏为主                    |
| 案例     | 大编辑式媒体与说明并置  | 仍保留大媒体     | 媒体后置/堆叠      | 顺序纵向，CTA 紧随摘要            |
| 卡片     | 最多三列                | 2–3 列           | 2 列               | 1 列                              |
| 表单     | 窄列居中                | 窄列居中         | 全宽安全区         | 单列、字段高 48px                 |
| 滚动叙事 | 允许少量 pin            | 只保留必要 pin   | 改普通进入动画     | 无 pin、无横滚、无 scroll-jacking |
| Hover/3D | 有条件启用              | 有条件启用       | 关闭倾斜           | 关闭倾斜、磁吸和自定义光标        |

### 9.3 不可妥协项

- 手机上不横向裁断标题、标签、按钮文字和表单说明。
- 任何触控目标不低于 44 × 44px。
- 关键 CTA 不能因为浏览器工具栏、固定底栏或全屏菜单被遮住。
- 在 375px 宽度时，正文、导航、菜单、表单和项目卡片都必须自然换行。
- 手机端宁可减掉装饰运动，也不能压缩文字、降低对比度或隐藏核心内容。

---

## 10. 动效系统

### 10.1 动效层级

| 级别    | 目的                       | 时长                 | 示例                      |
| ------- | -------------------------- | -------------------- | ------------------------- |
| L1 反馈 | 确认点击、输入、切换       | 120–220ms            | 按钮压下、Toast、焦点     |
| L2 进入 | 让内容有秩序出现           | 420–650ms            | 卡片/媒体 reveal          |
| L3 叙事 | 连接一个章节中的内容       | 随滚动进度           | 章节文本与图像协同        |
| L4 沉浸 | 提供独特首屏或一个案例高潮 | 560–800ms / 滚动驱动 | System Loom、一次页面过渡 |
| L5 环境 | 低权重背景生命感           | 12–20s               | 极缓慢的环境光/丝线漂移   |

### 10.2 时长与缓动令牌

| Token           | 值                             |
| --------------- | ------------------------------ |
| motion-instant  | 120ms                          |
| motion-fast     | 180ms                          |
| motion-standard | 240ms                          |
| motion-reveal   | 600ms                          |
| motion-chapter  | 720ms                          |
| motion-page     | 560ms                          |
| motion-ambient  | 16000ms                        |
| ease-out        | cubic-bezier(0.16, 1, 0.3, 1)  |
| ease-enter      | cubic-bezier(0.22, 1, 0.36, 1) |
| ease-in-out     | cubic-bezier(0.65, 0, 0.35, 1) |

GSAP 的滚动/时间轴可使用 power3.out 或等效缓动；Framer Motion 的微交互使用上述 CSS 缓动。后续不得在同一个页面混用互相矛盾的弹簧、弹跳和线性动画。

### 10.3 动画语言

- 进入：透明度从 0 到 1，垂直位移 12–20px；禁止默认从 40px 以上飞入。
- 大标题：只按行或语义块揭示；中文不逐字符拆分。
- 图像：可做一次 mask/裁切揭示或 1.03 的缓慢放大，不能两者叠加成持续抖动。
- 按钮：反馈短、明确、克制；核心 CTA 在桌面可有轻微磁吸，幅度不超过 8px。
- 页面切换：交接而非遮挡，最长 800ms；页面主体不应在动画结束前完全不可访问。
- 环境光：低不透明度、长周期，只在一处场景存在。

### 10.4 滚动与 GSAP 策略

- GSAP 只处理复杂时间轴、固定叙事和少数媒体遮罩；普通 reveal 不必强行使用 GSAP。
- 每页桌面端最多 1–2 个 pinned 叙事区块；首期项目展示采用大编辑式案例卡，不使用横向滚动。
- 不截获滚轮、不改变浏览器滚动方向、不强制用户完成动画才能继续。
- 未来实现时，每个 ScrollTrigger 和监听器都必须在组件卸载时销毁。

### 10.5 平滑滚动与设备能力

- Lenis 只提升手感，不改变锚点、键盘导航、文本选择、浏览器查找和辅助技术行为。
- prefers-reduced-motion: reduce 时不启用 Lenis、自动滚动、视差、磁吸、倾斜、无限跑马灯、连续 WebGL 动画和页面覆盖转场。
- hover: hover 且 pointer: fine 才允许 Cursor Glow、倾斜和磁吸；触摸设备不模拟鼠标效果。

---

## 11. System Loom / Three.js 预算与降级

Three.js 不是全站装饰框架，只服务于 Hero 的一次品牌记忆点。

| 项目          | 桌面                 | 平板           | 手机/低性能         |
| ------------- | -------------------- | -------------- | ------------------- |
| 加载方式      | 动态导入，非阻塞正文 | 动态导入，延迟 | 默认不加载          |
| 像素比        | 限制在 1–1.5         | 限制在 1–1.25  | 不使用 WebGL        |
| 粒子/节点预算 | 不超过 160           | 不超过 90      | 静态 SVG/海报或无图 |
| 交互          | 轻微鼠标视差         | 轻微/可关闭    | 无                  |
| 动画          | 低速、可暂停         | 更少元素       | 静态                |
| 失败回退      | 纯 CSS 环境光 + 文案 | 同左           | 同左或静态图        |

- 文字先渲染，场景后加载；场景异常不影响 CTA 和首屏可读性。
- 页面离开时应释放 WebGL 资源；不可让隐藏场景持续占用 GPU。
- 不在项目列表、博客列表、服务卡片中重复创建 Canvas。

---

## 12. 无障碍与可用性基线

目标为 WCAG 2.2 AA 的实用基线，而不是事后补丁。

- 使用语义化地标：header、nav、main、section、footer；提供“跳到主要内容”链接。
- 正文与控件的对比度满足 AA；禁用灰 text disabled 不承载必须阅读的内容。
- 键盘顺序与视觉顺序一致；所有可点击卡片、按钮、链接、菜单、对话框都可使用键盘。
- 焦点样式全站一致且不被 overflow: hidden 裁掉。
- 菜单、Dialog、灯箱都支持 Esc 关闭、焦点锁定与焦点回归。
- 所有媒体有准确 alt；装饰性 System Loom 标记为装饰，不被屏幕阅读器反复播报。
- 表单有可见 label、字段级错误、提交结果提示和服务端错误的预留位置。
- 动效默认不承载信息；reduced motion 下内容立即可见或使用简短淡入。
- 语言和字体回退要兼容中文、英文、代码和数字，不让中文因字体加载失败退化为难读字形。

---

## 13. 实现映射（供阶段三使用，当前不创建）

### 13.1 建议的未来文件职责

| 未来文件                  | 职责                                           |
| ------------------------- | ---------------------------------------------- |
| src/styles/tokens.css     | 所有色彩、间距、圆角、层级、动效 CSS Variables |
| src/styles/globals.css    | 全局背景、焦点、选择态、滚动与基础元素         |
| src/styles/typography.css | 字体加载后的字号与 prose 规则                  |
| src/styles/motion.css     | reduced motion 与统一关键帧                    |
| src/components/ui/        | Button、Input、Tag、Card、容器等基础组件       |
| src/components/layout/    | Navigation、Footer、Section、PageShell         |
| src/components/motion/    | Reveal、Stagger、TextReveal、能力检测          |
| src/components/three/     | System Loom 与静态回退，仅在需要时加载         |

### 13.2 Tailwind 映射建议

| 设计令牌族 | Tailwind 语义名称示例                                                   |
| ---------- | ----------------------------------------------------------------------- |
| 颜色       | canvas、canvas-subtle、surface、surface-raised、ink、muted、brand、cyan |
| 间距       | 直接映射 4px 基准，不另造 13/18/26 的魔法数                             |
| 字体       | font-display、font-sans、font-mono                                      |
| 圆角       | radius-xs、radius-sm、radius-md、radius-lg                              |
| 动效       | duration-fast、duration-reveal、ease-enter、ease-out                    |
| 容器       | content、wide、text、narrow                                             |

下面是文档示例，用于说明令牌结构；它不是本阶段写入项目的样式文件：

```css
:root {
  --color-canvas: oklch(0.115 0 0);
  --color-canvas-subtle: oklch(0.146 0.004 285.9);
  --color-surface-1: oklch(0.179 0.006 285.8);
  --color-text-primary: oklch(0.976 0.001 286.4);
  --color-text-secondary: oklch(0.772 0.011 286.1);
  --color-action-primary: oklch(0.555 0.22 280);
  --color-action-primary-hover: oklch(0.575 0.22 280);
  --color-accent-cyan: oklch(0.797 0.134 211.5);
  --space-4: 16px;
  --space-8: 32px;
  --radius-md: 14px;
  --motion-reveal: 600ms;
  --ease-enter: cubic-bezier(0.22, 1, 0.36, 1);
}
```

### 13.3 实现检查清单

后续新增任何界面前，检查：

1. 是否用了语义令牌而不是原始色值或魔法数？
2. 是否只有一个视觉主 CTA？
3. 无 Hover、无 JavaScript、reduced motion 时是否仍完整可读、可操作？
4. 375px、768px、1024px、1440px 时布局是否遵守本文件？
5. 是否为了“高级感”重复使用玻璃、渐变标题、过大圆角、粒子或卡片网格？
6. 图片、图标、状态、表单与动效是否有无障碍文字和键盘行为？

---

## 14. 视觉风险与控制策略

| 风险             | 早期信号                       | 处理策略                                           |
| ---------------- | ------------------------------ | -------------------------------------------------- |
| 深色页面过于单调 | 全站只有黑色卡片和白字         | System Loom、真实案例图、局部环境光建立章节差异    |
| 变成 AI 模板站   | 大渐变字、重复 badge、每区三卡 | 以大案例、文本节奏和明确转化代替通用组件拼贴       |
| 动效影响转化     | CTA 晚出现、滚动困难、手机掉帧 | 文案先渲染；移动端优先静态；控制 pinned/WebGL 数量 |
| 紫色滥用         | 每个标签、图标和边框都变紫     | 紫色仅主动作、焦点和少量系统线                     |
| “技术感”装扮化   | 满屏代码、等宽标签、网格背景   | 技术通过案例、架构图、结果和真实内容表达           |
| 表单看起来像后台 | 过多框、阴影、标签图标         | 单列、明确标签、足够留白、少量状态反馈             |
| 中文排版失控     | 标题溢出、英文与中文基线混乱   | 强制 clamp、语义换行、TextContainer 和字体回退     |

---

## 15. 阶段二验收结果

- 已确定品牌视觉方向、深色表面系统、主辅色职责与对比度约束。
- 已确定字体角色、字号比例、间距、网格、圆角、层级与图像策略。
- 已定义布局、文字、交互、内容、表单、反馈和动效组件的契约与状态。
- 已定义桌面、笔记本、平板、手机、低性能设备与 reduced motion 的行为。
- 已确定 System Loom 是唯一的高辨识度沉浸式视觉元素，并定义性能预算和静态回退。
- 阶段三已将 Token、全局样式、字体映射和基础组件落地到工程中；正式首页及业务页面仍未开始开发。

阶段二的设计约束继续作为后续实现基准。所有正式页面开发必须复用这些令牌、组件状态、响应式与 reduced motion 规则。

---

## 16. 阶段四基础层增量

本节记录阶段四新增的全站壳层契约；它不定义正式 Hero、案例、服务或博客内容。

### 16.1 全站壳层

- `SiteHeader` 固定在视口顶部：顶部透明；滚动后使用 canvas-subtle 作为无 backdrop-filter 时的可读降级，并只在支持时叠加 18px 模糊。
- Header 只通过 `transform` 隐藏或显示，不能改变文档流高度；距离顶部 80px 内始终保持可见。
- 桌面端在 `lg` 及以上显示完整导航和唯一的“联系合作”CTA；`lg` 以下切换为全屏菜单。
- 全屏菜单使用可访问 Dialog：菜单面板、关闭按钮、导航项和联系信息属于同一个焦点上下文；没有真实社交链接或邮箱时不渲染伪链接。
- `SiteFooter` 是全站结束区域：大型联系 CTA、品牌说明、来源一致的导航、当前年份和返回顶部组成三段结构；不在 Footer 加入 WebGL 或无意义环境动画。

### 16.2 新增复用组件

| 组件                          | 职责                               | 关键约束                                                            |
| ----------------------------- | ---------------------------------- | ------------------------------------------------------------------- |
| BrandLogo                     | 文本字标 `TONG.`                   | 点号可使用品牌色；返回首页；不使用外部品牌资产                      |
| NavigationLink                | 导航默认、hover、active 与键盘焦点 | active 用小圆点；hover 用短下划线；项目/博客子路由保持父级 active   |
| SectionHeading / Eyebrow      | 章节编号、标题、描述、行动入口     | 标题默认 h2；支持左/中对齐；不强制每节都有编号                      |
| Reveal / Stagger / TextReveal | 低权重的进入动画                   | 初始 HTML 保持可读；reduced motion 直接显示；中文只按行或语义块揭示 |
| PageTransition                | 路由内容的轻量交接                 | 仅 opacity + 12px 以内位移；不覆盖或阻断内容                        |
| SectionPlaceholder            | 阶段性首页结构验证                 | 只表达编号、名称与开发状态；不可伪装成正式 Hero 或业务内容          |

### 16.3 层级与锚点补充

阶段四统一使用以下层级，旧名称保留为兼容别名：

| Token                |  值 | 用途                   |
| -------------------- | --: | ---------------------- |
| z-base               |   0 | 页面底层               |
| z-content / z-raised |  10 | 普通内容与局部浮层     |
| z-sticky             |  20 | 局部固定内容           |
| z-header / z-nav     |  50 | 固定 Header            |
| z-popover            |  60 | 轻量浮层               |
| z-overlay            |  80 | 菜单遮罩               |
| z-modal              | 100 | 全屏菜单与 Dialog 内容 |
| z-toast              | 120 | 全局提示               |
| z-skip-link          | 150 | 键盘 Skip Link         |

所有锚点区块使用 `scroll-margin-top: calc(var(--navigation-height) + var(--space-8))`，并依赖原生滚动与 `scroll-padding-top`，而非为每个链接添加滚动脚本。

### 16.4 阶段四验收边界

- 本阶段可渲染主页的八个 Section 骨架与开发状态，但不填充正式 Hero 或任一业务 Section 内容。
- `/design-system` 是临时、`noindex` 的组件检查页；用于验证 Header、Footer、排版、动效和 reduced motion 基础。
- `/about`、`/projects`、`/projects/example`、`/blog`、`/blog/example`、`/services`、`/contact` 在本阶段只提供可访问的结构占位页；除 `example` 结构样例外，其余未知详情路径进入 404。

---

## 17. 阶段五 Hero 增量

### 17.1 首屏叙事与版式

- Hero 只承载“我是谁、我做什么、核心技术方向、下一步行动”四个信息层级；标题采用两行中文语义换行：`我把想法，` / `做成真正可运行的产品。`。
- 首屏使用 `min-height: max(45rem, 100svh)`；手机回退到 `100svh` 并以内容自然撑开。顶部安全区为导航高度加一档空间，底部为 CTA 和滚动提示保留空间。
- 桌面端使用文字左、System Loom 右的编辑式分栏；平板保留压缩视觉；手机以文字、CTA、技术行、状态为优先顺序，System Loom 不进入 WebGL 路径。
- Hero 标题使用 `--type-hero`：`clamp(2.75rem, 7.2vw, 7.5rem)`，行高 `0.98`、轻微负字距；它是阶段五唯一允许超过原 `display-xl` 上限的展示字号。

### 17.2 Hero 专用令牌与层级

| Token                       | 用途                                   |
| --------------------------- | -------------------------------------- |
| `--type-hero`               | 两行 Hero 主标题的响应式字号           |
| `--hero-min-height-desktop` | 桌面首屏最小高度，兼顾短屏与动态视口   |
| `--hero-min-height-mobile`  | 手机首屏最小高度                       |
| `--hero-grid-size`          | 低对比 CSS 网格的单元尺寸              |
| `--hero-scene-fade`         | System Loom 渐显时间，使用现有进入曲线 |

Hero 背景按 `z-base` 组织；System Loom 与静态 SVG 都处于内容之下且永远 `pointer-events: none`；Hero 文案、CTA 与状态使用 `z-content`；固定导航与移动菜单继续沿用阶段四层级，不因 Canvas 变化。

### 17.3 视觉语言与运动

- 背景由基础暗色、低透明紫/青环境光、局部网格、轻噪点和文字保护渐变组成。噪点不透明度不超过 `0.05`，不使用彩虹、渐变文字、HUD 或代码雨。
- System Loom 是抽象模块结构加预计算节点网络：6–12 个半透明模块、24–48 个节点和少量预定义连接线。它表达系统、连接与数据流，不模拟地球、银河或粒子爆炸。
- 文案按 eyebrow、标题行、描述、技术行、CTA、状态的顺序进入；动画只改变视觉状态，服务端初始 HTML 必须保留完整文本和可操作链接。中文仅按行揭示，不逐字。
- 精细指针且未开启 reduced motion 时允许轻微 CSS 光晕、场景视差和慢速呼吸；移动端关闭指针视差；`prefers-reduced-motion` 下关闭连续运动、遮罩位移和 WebGL，保留静态背景与全部文案。

### 17.4 性能、降级与无障碍契约

- R3F/Three.js 仅在通过 WebGL 检测的细指针桌面端动态导入；正文先渲染，加载中和失败时保留 CSS/SVG 静态构图。Canvas 使用限制后的 DPR，禁止 Bloom、后期处理和每帧节点距离计算。
- 可见标签页使用连续帧循环；隐藏标签页切换为 `frameloop="never"`，路由离开时由组件卸载清理 Canvas、监听器与 requestAnimationFrame。
- Hero 只包含一个完整、可读的 `h1`；视觉拆分行对辅助技术隐藏，Canvas/SVG 是装饰性内容。CTA 为真实链接，合作状态始终包含文本，滚动提示不是按钮。
- 当前已接受的边界：Safari、Firefox 的实际 GPU 表现依赖用户设备；移动端明确选择静态体验以优先保证标题、CTA、对比度和可交互性。

---

## 18. 阶段六 About 增量

### 18.1 章节职责与叙事

- 首页 About 是 Hero 后的第二章，只回答“我是谁、如何形成能力、如何工作”，不承担完整简历、客户案例或技术栈清单。
- 章节采用 `02 / ABOUT` 作为一次有语义的章节标记；四段成长路径自身使用 01–04，因为其顺序承载能力演进信息。
- 主陈述为“我不仅编写功能，也关注产品是否真正解决问题。”；辅助正文覆盖需求、产品、架构、开发、AI 接入和上线，但不虚构客户、公司、年限或项目数量。
- 视觉签名是“演进模块板”：以 `CORE`、`API`、`AUTH`、`DATA`、`UI`、`AUTO`、`RAG`、`AGENT` 表达 Java → 全栈 → Python 自动化 → AI 应用的能力扩展。它是抽象系统蓝图，不是第二个 WebGL 场景、后台截图或人物照片。

### 18.2 新增令牌

| Token                           | 用途                                                           |
| ------------------------------- | -------------------------------------------------------------- |
| `--about-statement-size`        | 比 Hero 更安静的品牌陈述字号，桌面不超过 80px，手机不低于 32px |
| `--about-statement-measure`     | 主陈述的最大阅读宽度，避免中文横跨整个桌面                     |
| `--about-story-copy-measure`    | 成长阶段正文的最大阅读宽度，保持双栏中可扫读的段落长度         |
| `--about-metric-copy-measure`   | 指标说明文字的最大阅读宽度，避免四列内文字拥挤                 |
| `--about-sticky-offset`         | 桌面 Story 视觉相对固定导航的安全顶部偏移                      |
| `--about-visual-height`         | 短屏也不会遮住 Story 内容的演进模块板高度                      |
| `--about-story-step-min-height` | 桌面每一段成长路径的可阅读滚动空间                             |
| `--about-visual-reveal`         | 模块板首次遮罩揭示的统一时长                                   |

这些令牌只服务 About 的编辑式叙事；色彩、边界、圆角、字族、层级和全局曲线全部复用既有语义令牌。

### 18.3 组件与 Server / Client 边界

| 组件                               | 职责                                             | 渲染边界                            |
| ---------------------------------- | ------------------------------------------------ | ----------------------------------- |
| `AboutSection`                     | 语义 section、章节标题和内容编排                 | Server                              |
| `AboutStatement`                   | 分行主陈述与解释正文                             | Client 叶组件；服务端 HTML 保持可读 |
| `AboutStory`                       | 桌面 Sticky Story、少量 ScrollTrigger 与激活索引 | Client 叶组件                       |
| `AboutStoryStep`                   | 单一成长阶段的语义列表项                         | 可复用展示组件                      |
| `AboutVisual`                      | 演进模块板及模块状态切换                         | Client 叶组件，纯 DOM + SVG         |
| `AboutMobileStory`                 | 手机/平板的纵向有序时间线                        | Server                              |
| `AboutMetrics` / `AboutMetricCard` | 能力型指标与一次性数字递增                       | Server 外壳 + Client 数字叶组件     |
| `AboutClosing`                     | 收束文案和 `/about` 文字链接                     | Server                              |

文案、指标、模块关系和成长路径全部存放在 `src/data/about.ts`，不在组件内硬编码整组数据。客户端只接收序列化后的静态数据，不发起请求。

### 18.4 布局、状态与响应式

- About 前半段使用 `canvas-subtle`，Story 内部以微弱表面差和细分隔线构成节奏，Metrics 回到 `canvas`；不切换纯白背景。
- `min-width: 1024px` 且非 reduced motion 时使用原生 CSS Sticky 左栏和右侧有序 Story；ScrollTrigger 只更新 `activeStepIndex`，不 pin 整个 section，也不逐帧写入 React state。
- Story 状态为 `inactive`、`active`、`completed`：非当前项仍可读，当前项强调编号、标题、关键词和模块板；快速/反向滚动与刷新中间位置均以 trigger 状态重新同步。
- 小于 1024px 时，桌面 Story 不渲染为可见内容，改为单列有序时间线；禁止 Sticky、Pin、横向滚动和高成本模块布局动画。
- Metrics 在桌面四列、平板两列、手机单列；其形式是带细顶边的编辑式信息列，不是 SaaS 卡片网格。

### 18.5 动效、性能与无障碍契约

- 主陈述按语义行做一次 mask reveal；其余内容以短位移 fade、模块 crossfade 和一次性数字递增组织。默认 HTML 从不因动画而隐藏。
- About 使用不超过六个 Story/视觉触发器，并通过 `gsap.context().revert()` 清理；不重新初始化 Lenis，不增加依赖，不使用新的 Canvas 或视频。
- `prefers-reduced-motion` 下不注册 Sticky 激活逻辑、无模块移动、无计数、无遮罩；所有 Story、视觉和最终指标立即可读。
- 语义结构为一个 `h2`、一个桌面或移动端可见的有序列表、各阶段 `h3`、正文、关键词列表和真实 `/about` 链接。模块 SVG 与辅助标签为装饰性内容并标记 `aria-hidden`。
- 200% 缩放、窄屏和无 JavaScript 下保持文案、成长路径、指标与链接可读；视觉绝不承载唯一信息。

### 18.6 已接受边界

- `/about` 独立页面仍保留现有占位状态：本阶段只实现首页 About，详情页内容留给后续阶段。
- CSS Sticky 在 Safari 的最终表现依赖真实设备；实现避免在其祖先上添加不必要的 `overflow`，并在小屏回退为静态时间线。

---

## 19. 阶段七 Skills 增量

### 19.1 章节职责与视觉签名

- 首页 Skills 是 Hero 与 About 之后的第三章，回答“这些能力怎样协作形成产品”；不展示百分比、认证、工作年限或彩色技术 Logo 墙。
- `03 / CAPABILITIES` 是章节标记；四个模块的 01–04 表达产品构成，而不是又一组装饰性栏目编号。
- 视觉签名为“能力控制台”：左侧是后端、前端、数据与 AI 四个可选择的能力域，右侧将当前域的用途、技术、应用场景和抽象产品关系图组织在同一阅读面内。
- 关系图的中心始终是 `PRODUCT / 可用的软件产品`，周围仅表示 BACKEND、FRONTEND、DATA、AI 的协作关系。它是低权重 DOM + SVG 说明，不是图表、Canvas、第二个 System Loom 或架构承诺。

### 19.2 新增令牌

| Token                            | 用途                                                 |
| -------------------------------- | ---------------------------------------------------- |
| `--skills-overview-measure`      | 能力总览主陈述的最大阅读宽度，避免双栏介绍在宽屏过长 |
| `--skills-sticky-offset`         | 桌面详情面板相对固定导航的安全顶部偏移               |
| `--skills-detail-min-height`     | 详情面板的稳定最小高度，避免组切换时明显跳动         |
| `--skills-map-height`            | 关系图的受控高度，保证短屏不产生嵌套滚动             |
| `--skills-group-card-min-height` | 左侧编辑式能力卡片的最小内容高度                     |
| `--skills-panel-transition`      | 当前能力内容 crossfade 的统一时长                    |

色彩仍复用现有语义令牌：后端使用克制品牌紫、前端使用青色、数据使用信息蓝、AI 使用辅助紫。色彩只提示当前模块，绝不承担唯一状态信息。

### 19.3 组件与数据边界

| 组件                    | 职责                                                              | 渲染边界                                 |
| ----------------------- | ----------------------------------------------------------------- | ---------------------------------------- |
| `SkillsSection`         | 语义 section 与章节编排                                           | Server                                   |
| `SkillsOverview`        | 能力主张与简洁覆盖流程                                            | Server，复用 Reveal                      |
| `SkillsDesktopExplorer` | Tabs、键盘切换与当前详情同步                                      | Client 叶组件                            |
| `SkillsHashAnchor`      | 在字体与 Skills 布局稳定后校正 `#skills` 初始定位与浏览器历史定位 | Client 叶组件，仅在 `#skills` 存在时运行 |
| `SkillGroupCard`        | 真实 tab button 的编辑式分组入口                                  | 展示组件，由 Client Explorer 驱动        |
| `SkillDetailPanel`      | 当前分组的说明、场景与技术列表                                    | 展示组件，由 Client Explorer 驱动        |
| `SkillsSystemMap`       | 抽象模块关系图                                                    | Client 叶组件，纯 DOM + SVG              |
| `SkillsMobileList`      | 小屏完整纵向内容                                                  | Server                                   |
| `SkillsClosing`         | 收束文案与 `/projects` 真实链接                                   | Server                                   |

`src/data/skills.ts` 是技术等级、描述、适用场景和关联关系的唯一来源。等级只使用“核心能力 / 项目使用 / 持续探索”，并在数据文件中标记为应随真实经验调整的内容。

### 19.4 布局、交互与降级

- 在 `min-width: 1024px` 时，桌面采用 7/5 的能力列表与详情面板分栏；详情仅在足够高的视口且非 reduced motion 时 Sticky，短屏自然随页面滚动，不创建嵌套滚动条。
- 四个分组采用完整 WAI-ARIA Tabs：真实 `button`、`tablist`、`tab` 与 `tabpanel`，支持 Tab、Enter/Space、方向键、Home、End，并以文字和 `aria-selected` 表示当前状态。
- `<1024px` 时隐藏桌面交互区，改为完整静态纵向模块；每组的说明、场景、技术名称、等级与简洁关系表达全部直接可读。无 JavaScript 的桌面通过 `noscript` 输出完整静态列表。
- 浏览器直接打开 `/#skills` 时，`SkillsHashAnchor` 等待字体就绪、一次短暂的原生锚点稳定窗口与两帧布局稳定后以无动画方式重新对齐目标；后续 `hashchange` 保留常规平滑体验，并尊重 reduced motion。它只处理该锚点，避免初始服务端定位早于客户端排版稳定而产生偏移。
- 卡片 hover 只提升表面、强调编号并右移箭头；焦点状态复用全局焦点环。没有 Pointer Glow、3D Tilt、无限运动或 Hover 才出现的关键信息。

### 19.5 动效、性能与无障碍契约

- Skills 只使用章节/总览 Reveal 与当前详情的短 crossfade；关系图仅更新当前节点与连线强调，不重播整张图。reduced motion 下所有内容立即显示、详情立即更新、无位移与连线绘制。
- 不引入新依赖、Canvas、Three.js 场景、图表库、技术 Logo 或全局 Pointer listener。Client 状态仅保存 `activeGroupId` 与用于键盘聚焦的按钮引用。
- 语义结构为一个 `h2`、四个 `h3`、技术和适用场景列表、Tabs 与真实项目链接；关系图完全标记为装饰，技术等级有清晰文字，不以颜色单独传达含义。

### 19.6 已接受边界

- `/projects` 仍为现有占位路由；阶段七只提供真实入口，不开发项目内容。
- 桌面 Sticky 依赖真实 Safari 设备做最终兼容性确认；实现不在 Sticky 祖先添加裁切 overflow，并在短屏、平板与 reduced motion 下回退为常规文档流。

---

## 20. 阶段八 Selected Projects 增量

### 20.1 章节职责与内容真实性

- 首页 Projects 是第四章，回答“我如何理解具体问题、设计方案并把它做成可运行的系统方向”；它不是案例数量、客户 Logo、成果数字或产品截图的陈列区。
- `04 / SELECTED WORK` 与前文能力章节形成连续叙事，但其编号只表达首页已完成的阅读章节，而不是装饰性编号网格。
- 首批四个条目为连锁门店运营管理系统、AI 智能问答平台、企业知识库 RAG 系统和卡密自动发货系统。所有条目明确显示“个人项目 / 概念阶段”，不暗示客户交付、上线指标、真实用户或商业结果。
- 预览区域是纯 DOM 的抽象界面构图，表达产品域与信息结构；没有真实数据、人物、品牌、截图或可被误认为真实后台的内容。若未来有可公开的真实素材，再以来源明确的图片或视频替换对应 Preview。

### 20.2 信息架构与组件边界

| 组件                                                | 职责                                               | 渲染边界                                            |
| --------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------- |
| `ProjectsSection`                                   | 语义 section、章节标题、四层编排                   | Server                                              |
| `ProjectsIntro`                                     | 方法线“从需求理解，到技术方案、开发实现与上线准备” | Server 外壳，复用轻量 Reveal                        |
| `FeaturedProjects` / `FeaturedProjectItem`          | 有序案例列表、标题、问题、方案、技术与真实链接     | Server 外壳；单项内容复用 Reveal                    |
| `ProjectMedia`                                      | 16:10 预览链接与可访问名称                         | Server                                              |
| `ProjectMediaReveal`                                | 一次性的遮罩与缩放进入                             | Client 叶组件；服务端初始 HTML 仍显示完整链接与预览 |
| `ProjectsHashAnchor`                                | 直接打开 `/#projects` 时等待布局稳定后校正锚点     | Client 叶组件，仅在匹配 hash 时运行                 |
| `ProjectPreview` 与四个 Preview 变体                | 商品化的抽象产品界面构图                           | Server、`aria-hidden` 装饰内容                      |
| `ProjectMeta` / `ProjectStatus` / `ProjectTechList` | 分类、年份、项目性质、状态、技术列表               | Server                                              |
| `ProjectsSummary` / `ProjectsClosing`               | 覆盖方向、收束陈述与两个真实下一步                 | Server                                              |

项目领域文案、状态、性质、排序、技术与 Preview 类型统一来自 `src/data/projects.ts`；标签映射统一来自 `src/config/project.ts`。`src/types/project.ts` 约束分类、状态、项目性质、配色与预览类型，避免把静态业务数据散落进组件。

### 20.3 新增令牌与布局规则

| Token                               | 用途                                          |
| ----------------------------------- | --------------------------------------------- |
| `--projects-intro-measure`          | 方法线正文的阅读宽度                          |
| `--projects-feature-gap`            | 宽屏 7/5 项目图文分栏间距                     |
| `--projects-feature-space`          | 四个案例之间的叙事留白                        |
| `--projects-media-ratio`            | 预览区域稳定 16:10 比例，避免加载或缩放时跳动 |
| `--projects-summary-measure`        | 收束说明的最大阅读宽度                        |
| `--projects-preview-control-height` | DOM Preview 内部控件的稳定高度                |
| `--projects-directory-measure`      | 最小项目路由的可读正文宽度                    |

- `<1024px` 使用固定的文档顺序：项目元信息与标题 → Preview → 描述 → 问题/方案 → 技术栈 → 案例链接。禁止横向滚动、卡片翻转、悬浮才出现的关键信息和 Sticky 预览。
- `≥1024px` 使用 7/5 编辑式双栏并交替 Preview 左右位置；标题和正文在同一阅读列，Preview 跨两行，以留白和细分隔线而非卡片网格建立节奏。
- 预览使用既有 `--radius-media`、表面与边界令牌；项目元信息、状态与技术名使用现有代码字族，正文和标题使用现有 UI/Display 字族。项目色只作为 Preview 和分类的辅助强调，状态永远有文字。

### 20.4 动效、性能与无障碍契约

- Preview 进入使用一次性、不拦截点击的覆盖层 `clip-path` mask reveal（约 1020ms）与遮罩由 `scale(1.055)` 回到 `scale(1)`；预览内容本身始终存在并可交互，标题/正文只做短距离 Reveal。没有 GSAP Pin、横向滚动、滚动抢占、逐标签触发或新的 Canvas。
- 精细指针设备的 hover 只轻微提升 Preview 边界、缩放媒体框并改变“查看案例”胶囊的表面；触摸设备通过普通链接获得同样的项目入口，核心信息始终可见。
- `prefers-reduced-motion` 下 Client Reveal 返回静态容器，CSS 也去除互动过渡；初始 HTML 不依赖 JavaScript 才能显示项目内容。
- 直接打开 `/#projects` 时，`ProjectsHashAnchor` 会在字体准备完成、短暂稳定窗口与两帧布局后以无动画方式对齐章节；后续 hash 变化延续平滑滚动并尊重 reduced motion。它只处理 Projects，避免把初始锚点定位早于客户端布局完成。
- 语义结构为一个 `h2`、四个带 `h3` 的 article、问题/方案定义列表、技术无序列表、摘要列表和正常可聚焦的 Next Link。抽象 Preview 标记为装饰，链接有项目名称的可访问标签。
- 不引入图像、视频、Canvas、第三方请求或新依赖；16:10 预览尺寸由 CSS 预留，避免 CLS。路由级静态参数和服务端元数据由同一项目数据生成。

### 20.5 最小项目路由与已接受边界

- `/projects` 现在提供项目方向目录，并直接链接四个已知 slug；它不是筛选器、数据库、项目管理后台或完整作品集。
- `/projects/[slug]` 使用项目数据生成静态参数和基础 SEO，未知 slug 进入 404。详情页只展示分类、年份、真实性质、状态、简短说明与“完整项目案例将在后续阶段完善”，并保持 `noindex`，避免把未完成内容伪装为完整案例。
- 本阶段在项目区完成后停止：Services、Process、Blog Preview、Contact CTA、完整项目案例、真实素材和筛选能力均留给后续阶段。

---

## 21. 阶段九 Services 增量

### 21.1 章节职责与内容真实性

- 首页 Services 是第五章，回答“我能以什么方式帮助一个项目从问题走向可执行方案”；它服务于沟通转化，而不是服务套餐、价格表或能力炫示。
- `05 / SERVICES` 只列出六个方向：企业管理系统、AI 应用、Python 自动化、全栈开发、系统升级与维护、技术咨询与产品方案。每个方向都来自 `src/data/services.ts`，不在组件中复制业务数据。
- 不展示价格、交付周期、客户名称、案例数量、保证性成果或未经证实的能力承诺。AI 方向明确说明输出需要校验、RAG 只能降低而非消除错误；系统维护方向明确先评估现有代码、环境与范围。
- 联系入口只将白名单化的服务分类带到 `/contact?service=...`。Contact 在本阶段仅为安全的沟通占位页：不提供表单、API、数据库、邮件发送或隐私承诺。

### 21.2 组件与数据边界

| 组件                                                       | 职责                                      | 渲染边界                            |
| ---------------------------------------------------------- | ----------------------------------------- | ----------------------------------- |
| `ServicesSection`                                          | 语义章节、章节标题、渐进增强编排          | Server                              |
| `ServicesIntro` / `ServicesEngagement` / `ServicesClosing` | 开场、合作方式与收束链接                  | Server，复用 `Reveal`               |
| `ServicesDesktopExplorer`                                  | 当前服务状态、Tabs 键盘操作与桌面详情同步 | Client 叶组件                       |
| `ServiceList` / `ServiceListItem`                          | 有序服务入口与完整 ARIA Tabs 语义         | 由 Client Explorer 驱动             |
| `ServiceDetailPanel`                                       | 当前服务的边界、交付、技术方案与咨询入口  | Client 展示组件                     |
| `ServiceMobileAccordion`                                   | 小屏单开手风琴与自然阅读顺序              | Client 叶组件                       |
| `ServiceStaticList`                                        | 无 JavaScript 时所有服务的完整静态内容    | Server，置于 `noscript` 回退        |
| `ServicesHashAnchor`                                       | 在 `#services` 初始定位稳定后校正锚点     | Client 叶组件，仅匹配该 hash 时运行 |

`src/types/service.ts` 定义服务类别、强调色和内容模型；`src/config/service.ts` 是联系参数的唯一白名单解析与中文标签映射。公共标题、开场、合作方式与结尾文案集中在 `src/data/services.ts`。

### 21.3 新增令牌与布局规则

| Token                                     | 用途                           |
| ----------------------------------------- | ------------------------------ |
| `--services-intro-measure`                | 服务开场主陈述的受控阅读宽度   |
| `--services-intro-detail-measure`         | 开场辅助说明的最大阅读宽度     |
| `--services-list-row-min-height`          | 桌面服务入口稳定行高           |
| `--services-accordion-trigger-min-height` | 移动端触控入口的最小高度       |
| `--services-detail-min-height`            | 桌面详情切换时的稳定最小高度   |
| `--services-sticky-offset`                | 详情面板相对固定导航的安全偏移 |
| `--services-panel-transition`             | 当前服务详情的受控切换时长     |

- `≥1200px` 采用 7/5 的编辑式服务列表与详情面板：左侧是连续的全宽行，而不是卡片网格；右侧详情只有在视口高度至少 `54rem` 且非 reduced motion 时 Sticky。
- `<1200px` 改为单栏手风琴，默认展开第一项；内容不依赖 hover，用户可逐项展开或收起。`<768px` 的交付与技术列表保持单列，避免横向溢出。
- 开场在 `≥768px` 使用 7/5 两栏，合作方式在同一断点变为三列。无 JavaScript 时，`noscript` 显示完整的六项静态服务列表，不只保留首项。
- 服务色仅是弱辅助强调；当前状态同时通过 `aria-selected`、文字和按钮状态表达。服务区域复用现有深色画布、细边框、代码字族和按钮令牌，不引入插画、图片、视频、Canvas 或新依赖。

### 21.4 动效、性能与无障碍契约

- 桌面只在选择服务后对详情内容做约 320ms 的短 crossfade；移动端仅对当前手风琴高度和透明度做受控变化。没有横向滚动、全局指针监听、3D 倾斜、自动轮播或由 hover 驱动的内容切换。
- `prefers-reduced-motion` 下详情和手风琴立即更新，CSS 也移除过渡；章节正文与服务详情均在服务端 HTML 中存在，JavaScript 只增强交互。
- 桌面使用真实 `tablist`、`tab`、`tabpanel`，支持原生 Tab/Enter/Space 以及方向键、Home、End；移动端使用带 `aria-expanded` 与 `aria-controls` 的真实按钮。标题层级为一个 `h2`、每项一个 `h3`、详情分组为 `h4`，技术与交付内容均为语义列表。
- `ServicesHashAnchor` 在字体准备、短暂布局稳定窗口和两帧后无动画修正直接打开的锚点；后续 hash 变化延续常规平滑滚动并尊重 reduced motion。它不干预其他章节或页面导航。

### 21.5 已接受边界

- `/services` 仍保持既有占位路由；阶段九仅完成首页 Services，不扩展独立服务详情、项目筛选、流程章节、博客、完整 Contact 表单或持久化能力。
- 桌面 Sticky 的最终 Safari 设备兼容性、真实咨询渠道、可公开案例、服务价格与交付周期，均在具备真实业务资料后再确认，当前不以占位信息补齐。

---

## 22. 阶段十一 Blog Preview 增量

### 22.1 章节职责与数据边界

- 首页 Journal 是第七章，回答“我如何持续总结技术、产品和开发过程中的判断”；它展示三篇精选文章，不模拟完整内容平台、资讯流或社交互动。
- 文章元数据集中在 `src/content/blog/posts.ts`，三篇 MDX 文件只提供后续内容管线的 frontmatter 与正文骨架；组件不复制标题、标签、日期或文案。
- 每张卡片都以文章标题、分类、阅读时间、日期、摘要、标签和真实链接组成。没有浏览量、点赞、评论、客户成果或虚构封面素材。

### 22.2 布局、动效与可访问性

- `≥1200px` 为一篇由数据中的 `isPrimary` 标记确定的跨两列主文章及下方两篇次文章；主文章采用 7/5 图文分栏。`768px–1199px` 保持两列阅读顺序，手机端回退为单列自然文档流，不产生横向滚动。
- 封面是纯 DOM 的抽象系统图形，统一 `aria-hidden`；实际内容使用带 `h3` 的 `article` 和完整可聚焦链接，不依赖 hover 才能取得任何信息。
- 章节内容以 500–700ms 的短 Fade Up 和 80ms 交错进入；封面以一次性 clip-path/scale 揭示，精细指针 hover 仅将封面放大到 `1.03`。reduced motion 下客户端媒体揭示返回静态内容，CSS 同时取消交互过渡。
- 新令牌限定文章开场阅读宽度、16:9 封面比例、主卡稳定高度、卡片过渡与详情正文宽度；不引入图片、视频、Canvas、MDX 解析依赖或新的动画库。

### 22.3 最小文章详情与已接受边界

- `/blog/[slug]` 从同一文章数据生成静态参数、标题、描述、关键词、作者以及 Open Graph article 时间字段；未知 slug 返回 404。
- 详情页在正文 MDX 阅读体验完成前仅显示元信息与明确的建设说明，并保持 `noindex`，不将未完成内容伪装成可索引的完整文章。
- `/blog` 仍是既有的导航占位页；阶段十一在首页预览、基础详情和 MDX 目录结构完成后停止，不开发文章列表、搜索、评论、订阅、RSS、CMS 或 Open Source 章节。

---

## 23. 阶段十二 Open Source / Social Proof 增量

### 23.1 章节职责与内容真实性

- 首页 Ecosystem 是第八章，回答“我如何持续积累可公开的代码、项目实验与技术输出”；它不是 GitHub 数据面板、贡献热力图、社交账号墙或客户背书区。
- `08 / ECOSYSTEM` 使用“持续构建，持续分享，持续沉淀。”作为章节主张，按 GitHub / Code Identity → Open Source / Experiments → Technical Activity → Social Links → 收束文案组织阅读顺序。
- `src/data/open-source.ts` 是本章文案、技术活动与未来项目条目的唯一来源。当前 `openSourceProjects` 明确为空数组，页面用“正在整理可公开的实验和学习项目”说明真实状态，不伪造仓库、星标、贡献、客户、头像、项目数量或链接。
- `src/config/social.ts` 集中管理 GitHub、邮箱、LinkedIn、X、小红书与抖音。未配置的账号保持 `enabled: false` 与空 URL，不渲染为可点击入口；GitHub 个人主页也只在真实 URL 配置后才生成外链。

### 23.2 组件与数据边界

| 组件                                | 职责                                                      | 渲染边界                   |
| ----------------------------------- | --------------------------------------------------------- | -------------------------- |
| `EcosystemSection`                  | 章节、阅读顺序、数据编排                                  | Server                     |
| `GithubIdentity`                    | GitHub 介绍与开发者身份卡                                 | Server                     |
| `EcosystemVisual`                   | Repository → Modules → Systems → Products 的装饰性 DOM 图 | Server，`aria-hidden`      |
| `OpenSourceList` / `OpenSourceItem` | 可公开项目的语义列表与安全外链                            | Server，单项使用 `article` |
| `TechnicalActivity`                 | Writing / Building / Learning 的持续方向                  | Server                     |
| `SocialLinks`                       | 已配置真实链接或诚实的配置提示                            | Server                     |
| `EcosystemClosing`                  | 无 CTA 的长期价值收束                                     | Server                     |

`src/types/social.ts` 与 `src/types/open-source.ts` 定义可维护的数据契约，所有属性为 readonly。未来新增真实项目时，只需在 `src/data/open-source.ts` 补全项目数据；未来新增真实社交链接时，只需在 `src/config/social.ts` 填写 URL 并启用对应条目。

### 23.3 令牌、布局与交互规则

| Token                                | 用途                                   |
| ------------------------------------ | -------------------------------------- |
| `--ecosystem-intro-measure`          | 章节与子章节的最大阅读宽度             |
| `--ecosystem-visual-min-height`      | Code Activity Map 的稳定视觉高度       |
| `--ecosystem-project-row-min-height` | 未来开源项目横向条目的最小稳定高度     |
| `--ecosystem-interaction-transition` | 链接箭头、项目边界与标签强调的统一过渡 |

- `≥1024px` 使用 GitHub Identity 与 Code Activity Map 的 7/5 编辑式分栏；项目、技术活动和社交内容在下方保持连续纵向阅读。`768px–1023px` 内部身份卡和项目行适度分栏，`<768px` 恢复为 GitHub Identity → Visual → Open Source → Technical Activity → Social 的单列顺序。
- Code Activity Map 是自建 DOM 图，不使用 GitHub iframe、API、Canvas、图表库或贡献热力图；其中的系统链和节点只表达信息组织方向，并非实际统计数据。
- 精细指针 hover 仅强调未来项目边界和技术标签，并让外链箭头轻微位移；没有 3D 倾斜、自动轮播、无限动画或必须 hover 才能读取的信息。`prefers-reduced-motion` 下所有 Reveal 和交互过渡均降级为静态内容。

### 23.4 无障碍、性能与已接受边界

- 一个章节 `h2` 下使用清晰的 `h3` / `h4`、列表和语义 `article`；Visual 完全是装饰并标记为 `aria-hidden`。未来真实外链会使用正常可聚焦的 `<a>`，HTTP(S) 地址添加 `target="_blank"` 和 `rel="noreferrer"`。
- 不引入 GitHub API、身份认证、第三方请求、新依赖、图像、视频或客户端全局监听；全部内容直接由服务端 HTML 输出，避免 CLS 与首屏数据等待。
- 本阶段仅完成首页 Ecosystem；不开发 Contact CTA、联系页面、表单、邮件发送、订阅、登录、评论、CMS 或社交平台集成。真实 GitHub 主页、可公开项目、社交账号和头像均留待资料具备后再配置。

---

## 24. 阶段十三 Contact CTA 与联系表单增量

### 24.1 章节职责、主路径与内容真实性

- 首页 Contact 是第九章，也是首页的唯一最终行动入口。它用“有一个项目想法？让我们把它变成真正可运行的产品。”引导访问者到 `/contact`，并保留一个 `/projects` 的次级阅读入口；不展示价格、销售承诺、虚构联系方式或多余 CTA。
- `/contact` 的主路径是“说明项目目标 → 选择服务方向 → 留下可联系信息 → 获得明确的提交反馈”。它不承诺自动报价、即时回复或数据已持久化；当前环境未配置通知服务时，API 只完成服务端校验与基础防垃圾判断，为后续 Resend/SMTP 与持久化适配器保留边界。
- 真实账号仍从 `src/config/social.ts` 读取：未启用的 GitHub、邮箱、小红书、抖音和 LinkedIn 不生成假链接。失败状态只有在真实邮箱配置后才显示备用邮件入口。

### 24.2 目标用户与可访问性约束

- **移动端创业者**：从短视频或社交内容进入，快速判断能否咨询并在一屏内找到表单入口；不要求阅读长说明或依赖 Hover。
- **键盘用户**：每个字段都有显式 Label、可见焦点环、关联错误文本和逻辑 Tab 顺序；提交结果通过 `aria-live` 宣告。
- **需要低认知负担的咨询者**：字段按“身份 → 项目类型 → 预算与时间 → 项目描述”分组，预算明确只用于沟通而不是报价；错误文案说清要修正什么。
- **减少动态偏好用户**：标题和容器 Reveal 直接显示，字段不逐个入场，不用闪烁、跳动、自动轮播或计时焦虑。

### 24.3 组件、状态与数据边界

| 组件                                  | 职责                                              | 渲染边界              |
| ------------------------------------- | ------------------------------------------------- | --------------------- |
| `ContactCta`                          | 首页第九章的主张、说明和两个真实入口              | Server，复用 `Reveal` |
| `ContactForm`                         | React Hook Form 状态、API 提交、成功与失败恢复    | Client 叶组件         |
| `ContactField` / `TextareaField`      | 可访问 Label、控件、提示与字段错误                | 由 Client Form 驱动   |
| `ServiceSelect` / `BudgetSelect`      | 服务预选择与受控下拉字段                          | 由 Client Form 驱动   |
| `SubmitButton` / `SuccessMessage`     | 提交、禁用、加载、成功与失败反馈                  | 由 Client Form 驱动   |
| `ContactInformation` / `ContactNotes` | 真实联系方式、合作说明和三项 FAQ                  | Server                |
| `/api/contact`                        | JSON 边界解析、Zod 校验、防垃圾检查与通知准备状态 | Server Route          |

`src/types/contact.ts` 定义内部数据与提交结果；`src/lib/validations/contact.ts` 是客户端 resolver 和服务端 Route 共用的唯一输入边界。表单组件不得自行复制枚举、长度限制或服务名称。

### 24.4 新令牌、布局与动效

| Token                        | 用途                                    |
| ---------------------------- | --------------------------------------- |
| `--contact-cta-copy-measure` | 首页 CTA 主张与说明的最大阅读宽度       |
| `--contact-cta-min-height`   | 大留白 CTA 在宽屏的稳定最小高度         |
| `--contact-form-measure`     | 联系页面表单列的最大宽度                |
| `--form-control-height`      | 输入、Select 与提交按钮的最小可触摸高度 |
| `--contact-form-transition`  | 控件边界、表面与反馈的统一短过渡        |

- 首页 CTA 使用安静的 `--color-canvas-subtle` 基底、很弱的品牌环境光、细边界和宽阔留白；不使用大渐变、营销横幅、插画或玻璃卡片堆叠。
- `≥1024px` 的 Contact 页面采用 5/7 左右分栏：左侧是目的、真实联系方式与合作说明，右侧是表单。`<1024px` 回退为标题 → 联系方式 → 表单 → FAQ 的自然文档流；所有控件在手机端至少 `--form-control-height`，正文和输入文本不小于 16px。
- CTA 按标题 → 说明 → 按钮 → 背景光以 500–800ms 的短 Reveal 呈现；页面标题、信息区和表单容器使用同样的轻量 Fade Up。字段不逐个动画。reduced motion 下 `Reveal` 和 CSS 过渡均立即稳定。

### 24.5 表单交互、SEO 与接受边界

- 每个输入控件均有 Default、Focus、Error、Disabled 状态：Focus 使用品牌色焦点环，Error 同时使用文字、图标和 `aria-describedby`，不只改变 placeholder；成功是整张表单状态而非给每个字段着色。
- Honeypot `website` 与 `formOpenedAt` 仅在提交请求中出现，不对普通用户可见；服务端在 JSON 边界执行 Zod 校验、长度限制、内容类型检查和最短填写时间检查。客户端禁用提交按钮以避免一次会话内重复提交。
- `/contact` 使用专属 Metadata、canonical 与 Person JSON-LD；`sameAs` 和 `email` 仅在真实配置存在时填入。
- 本阶段不建立数据库、CRM、用户系统、邮件发送服务、验证码、在线聊天或自动报价。通知环境变量写入 `.env.example`，但未配置真实 Resend/SMTP 传输层前不会发出邮件；这是已记录的功能边界，不以“已送达”暗示替代。

---

## 25. 阶段十五最终视觉打磨增量

### 25.1 全站统一规则

- 常规 Button 统一为 48px，主要 CTA 统一为 56px；保留既有 `radius-control`、边界、焦点环与 180ms 交互曲线，不使用大缩放或弹跳。
- 页面进入交接使用 opacity 与 8px 位移，时长 360ms；路由加载态提供简洁、静态的内容轮廓，避免只显示细小进度条导致整屏空白，也不使用全屏 Loading。
- 移动菜单面板进入时长为 420ms，单个导航项为 300ms、40ms 交错；最后一个菜单项在 500ms 内稳定，关闭保持快速且不妨碍 Escape、焦点陷阱与滚动锁定。

### 25.2 鼠标与动效边界

- Hero 继续是唯一拥有桌面 Cursor Glow 与 WebGL 视差的叙事场景；光晕只在细指针、非 reduced motion 时更新，文本安全区、CTA 和链接层级始终更高。
- Magnetic interaction 只赋予首页 Contact CTA 的主按钮，细指针设备的最大位移为 4px；触摸、键盘与 reduced motion 下退化为普通可聚焦链接。
- 保留有明确含义的可用状态呼吸点与滚动提示；不增加全站跟随光标、悬浮旋转、长 Loading、全屏页面擦除或重复 WebGL。

### 25.3 最终审查范围

- 优先检查桌面首页的首屏信息层级与 Contact CTA，以及 375px 的 Hero、导航、中文标题、表单和 Footer；其他路由保持既有内容范围，不在本阶段追加业务页面。
- 性能审查继续以动态导入的 Hero Canvas、受限 DPR、隐藏标签页停帧、移动端静态回退和 token 驱动 CSS 为基础。数值型 Lighthouse 与实际 Vercel 发布仅在具备部署目标和专门审计环境时执行，不能编造结果。

---

## 26. 阶段十六 Tong Assistant 增量

### 26.1 角色与信息边界

- `Tong Assistant` 是站内项目顾问，不是通用聊天机器人；它只帮助访客理解瞳瞳的技术方向、项目案例、服务方式、文章与合作入口。
- 入口默认收起，由访客主动打开；不使用自动弹窗、未读红点、全屏聊天页或持续抢占注意力的动效。
- 回答必须由站内知识库支持。没有依据时明确说明，并引导到 `/contact`，不得编造客户、成果、价格、固定周期、工作经历或未展示的项目。
- 所有内部导航链接都由白名单知识条目提供；模型文本不能直接生成或控制未知 URL。

### 26.2 新增令牌与组件契约

| Token                            | 用途                               |
| -------------------------------- | ---------------------------------- |
| `--assistant-launcher-size`      | 悬浮入口尺寸；手机 52px、桌面 56px |
| `--assistant-window-width`       | 桌面聊天窗口宽度，400px            |
| `--assistant-window-height`      | 桌面聊天窗口高度，600px            |
| `--assistant-drawer-max-height`  | 手机 Drawer 的安全最大高度         |
| `--assistant-message-max-inline` | 单条消息的可读宽度                 |
| `--assistant-transition`         | 入口、窗口和消息反馈的统一短过渡   |

| 组件                | 职责                              | 渲染与无障碍约束                                                  |
| ------------------- | --------------------------------- | ----------------------------------------------------------------- |
| `AssistantLauncher` | 管理入口、按需加载窗口与会话状态  | Client 叶组件；窗口模块只在首次打开后加载                         |
| `AssistantButton`   | 固定右下角的主动入口              | 真实 button、可访问名称、桌面 56px / 手机 52px                    |
| `AssistantWindow`   | 桌面 Popover 与手机 Bottom Drawer | Radix Dialog、`role="dialog"`、Esc、焦点陷阱与关闭后焦点回归      |
| `ChatMessage`       | 用户/助手消息和安全的内部页面链接 | 不渲染 HTML；消息有清晰 role 与来源语义                           |
| `ChatInput`         | 短问题输入与流式发送状态          | 可见或屏幕阅读器 label；Enter 发送、Shift+Enter 换行、2000 字限制 |

### 26.3 布局、状态与动效

- 桌面窗口固定于右下角，尺寸为 400 × 600px；采用 `surface-1`、细边界和一层轻阴影，不做营销式大渐变或多层玻璃。
- 小于 `768px` 时转为底部 Drawer：左右保留安全边距，顶部圆角，最大高度受动态视口约束；不强行全屏，不遮挡关闭与输入操作。
- 消息区按“欢迎信息 → 对话 → 快捷问题 → 输入”组织。快捷问题帮助首次访问者开始，但不替代站内正文和正常导航。
- 入口 Hover 只提升表面、边界和很弱的环境光；窗口打开只使用 opacity + transform 的短过渡。`prefers-reduced-motion` 下不做缩放、光晕或弹出位移，内容直接稳定出现。
- 助手入口使用 `z-popover`；Dialog 遮罩和面板分别沿用既有 `z-overlay` 与 `z-modal`，不得压过 Skip Link 或破坏移动导航的焦点层级。

### 26.4 隐私、性能与已接受边界

- 会话仅保存在浏览器 `sessionStorage`，不保存邮箱、联系表单字段、IP 或服务端对话日志。服务端的基础限流仅保留进程内、不可逆的短期请求标识，不写入数据库或日志。
- 模型密钥只在服务端 Route 使用，不能出现在客户端包、响应或错误信息中。未配置模型时使用受限的站内知识引导；已配置模型失败时显示简短的联系页降级提示。
- API 最大请求体和单条问题均受长度限制，历史消息受数量限制，模型输出有上限和超时；疑似 Prompt 注入、索取内部提示词/密钥或非站内问题不进入模型。
- 助手代码通过 `next/dynamic` 在首次主动打开时加载；首屏不请求模型、不读取 sessionStorage、不加载聊天窗口模块。无 JavaScript 时不影响网站正文、SEO、导航或联系表单。

---

## 27. 阶段十七 CMS 后台增量

### 27.1 视觉角色与边界

- `/admin/*` 是内容工作台而非营销页面：保留官网深色画布、字体、边界和焦点系统，但采用更紧凑、安静的开发者工具密度。
- 后台不显示官网 Header、Footer 或 AI 悬浮入口；根布局通过 `SiteShell` 保持公开站点和后台的 Chrome 分离。
- 后台禁止收录，登录、未配置数据库和无数据状态都必须给出明确的下一步，不能用虚构统计或示例客户内容填充。

### 27.2 新增令牌与组件契约

| Token                          | 用途                             |
| ------------------------------ | -------------------------------- |
| `--admin-sidebar-width`        | 宽屏固定侧边栏宽度               |
| `--admin-header-height`        | 工作区顶部栏稳定高度             |
| `--admin-page-max-inline`      | 后台内容最大宽度                 |
| `--admin-control-height`       | 后台输入与按钮最小高度           |
| `--admin-form-measure`         | 表单可读宽度                     |
| `--admin-table-row-min-height` | 数据表行的最小触摸高度           |
| `--admin-transition`           | 侧栏、状态与表面反馈的统一短过渡 |

| 组件             | 职责                                   | 约束                                         |
| ---------------- | -------------------------------------- | -------------------------------------------- |
| `AdminShell`     | 宽屏 Sidebar、移动 Drawer 与工作区框架 | 仅后台路由使用；Dialog 管理移动焦点与 Escape |
| `AdminSidebar`   | 后台信息架构与当前路由状态             | 使用真实链接和 `aria-current`                |
| `AdminHeader`    | 导航入口、当前身份与退出操作           | 不显示私人资料以外的虚构账号数据             |
| 管理页表单与表格 | 内容创建、更新、状态与删除             | 真实 label、可见反馈、删除须确认             |

### 27.3 响应式、动效与可访问性

- `≥1024px` 使用固定 Sidebar 与单列工作区；`<1024px` 将 Sidebar 收入 Radix Drawer，Header 的菜单按钮成为唯一入口。
- 仅允许表面、边界和 Drawer 的 opacity/transform 短过渡；不为数据刷新、加载状态或非交互行添加装饰性动画。reduced motion 下直接稳定显示。
- 表格在窄屏转换为可横向滚动的内容容器；每个操作均可通过键盘到达，状态不只依赖颜色传达。

### 27.4 数据、隐私与已接受边界

- CMS 使用 PostgreSQL + Prisma；未配置 `DATABASE_URL` 时不创建数据库客户端，后台登录页显示配置提示，公开站点继续使用既有静态内容。
- 联系表单只在数据库已配置时持久化为 `Message`；未配置时保持既有安全校验与成功反馈，不伪称已写入 CRM。
- AI 知识库优先读取数据库的已启用条目，数据库未配置、空库或短暂不可用时回退 Markdown。向量同步需要独立 Embedding 与 pgvector 配置，首期不得伪造已生成向量。
- Vercel Blob 上传只在 `BLOB_READ_WRITE_TOKEN` 存在时开放；后台密钥、数据库 URL、密码哈希与模型密钥永不进入客户端包。

---

## 28. 阶段十八 数据分析与增长系统增量

### 28.1 分析边界与数据流

- 生产环境异步加载 `@vercel/analytics`，用于 Vercel 平台的匿名访问洞察；公开站点的转化与内容事件统一经过 `src/lib/analytics.ts`，组件不得直接接入第三方 SDK。
- `AnalyticsProvider` 在公开路由的客户端 hydration 后记录页面访问，并识别项目与文章详情路由；开发环境、`/admin` 路由和启用浏览器 `Do Not Track` 的访问都不会发送站内事件。
- `/api/analytics` 有 4 KB 请求体上限、严格 Zod 白名单和会话级内存限流；它只短暂使用浏览器会话 ID 限流，绝不写入数据库。数据库未配置时返回空成功响应，不产生虚构仪表盘数据。
- `AnalyticsEvent` 只保存事件名、路径、经筛选的 metadata 和时间；`/admin/analytics` 从这张表聚合 PV、排行与匿名漏斗。访问量不被伪装成精确 UV，因为本站不建立持久访客识别。

### 28.2 事件、UTM 与隐私

- 核心事件为 `page_view`、`view_project`、`read_article`、`view_service`、`click_contact`、`submit_contact` 与 `use_ai_assistant`；补充 `article_engagement` 和 `project_action` 为后续文章质量、GitHub/Demo/联系动作预留真实数据边界。
- UTM 仅允许 `utm_source`、`utm_medium`、`utm_campaign` 的受限安全值，并且只以 `firstSource`、`firstMedium`、`firstCampaign` 保存到本次浏览器会话。联系来源同样只保留会话级入口标签。
- AI 分析仅上传预定义分类（项目、服务、技术、合作或一般咨询）；问题内容、聊天历史、联系表单正文、邮箱、IP、设备指纹和密码均被 API 契约拒绝。
- `/privacy` 以普通可访问页面说明用途、范围、会话来源、Do Not Track 和联系入口；Footer 有固定链接，站点地图也包含此路由。

### 28.3 后台呈现与响应式

- `/admin/analytics` 受既有管理员布局和中间件保护，提供今日/7 天/30 天切换、今日/本周/本月访问量、项目/文章/服务排行、联系来源、AI 分类、热门页面及五步匿名事件漏斗。
- 图表使用轻量 CSS 柱状趋势图，不引入额外 BI/图表运行时；卡片、排行和漏斗使用既有后台令牌，在手机端自然单列，在宽屏形成两列排行与主图分栏。
- 零数据状态明确写为“还没有事件”，不会填入示例客户、虚构访问量或模拟转化率。文章停留与完读仅在访客自然离开文章或页面切换时记录聚合数值，不保留原文。

---

## 29. 阶段十九内容增长系统增量

### 29.1 内容体验与增长边界

- `/blog` 保持阅读优先：搜索、分类和标签是可访问的本地筛选工具，筛选结果不替代文章摘要、发布时间或正常路由；搜索不上传读者输入。
- 文章详情采用“摘要 → 目录 → 正文 → 关联文章/项目/服务 → 联系”的单列叙事。内部跳转应帮助读者继续解决问题，而不是用弹窗、倒计时或伪造社会证明打断阅读。
- CMS 的内容日历、关键词库和 AI 草稿器属于后台工作台。AI 只生成可复制的候选文案，绝不自动写入、自动发布或虚构经历、客户、结果和数据。
- 内容增长看板只聚合已收集的匿名事件；数据库未配置或没有事件时显示真实空状态，不使用示例访问量或转化率。

### 29.2 新增令牌与组件契约

| Token                                   | 用途                        |
| --------------------------------------- | --------------------------- |
| `--content-directory-control-height`    | 博客搜索输入的稳定触摸高度  |
| `--content-directory-filter-transition` | 搜索与筛选的短反馈过渡      |
| `--content-code-max-block-size`         | 长代码块的可滚动安全高度    |
| `--content-related-grid-min`            | 关联内容卡片的最小可读宽度  |
| `--content-calendar-date-size`          | 后台内容日历日期锚点尺寸    |
| `--content-ai-result-max-block-size`    | AI 草稿结果的最大可滚动高度 |

| 组件                 | 职责                                   | 约束                                                      |
| -------------------- | -------------------------------------- | --------------------------------------------------------- |
| `BlogDirectory`      | 本地搜索、分类和标签筛选               | Client 叶组件；不上传搜索词，所有关键文章信息不依赖 Hover |
| `MarkdownContent`    | Markdown、表格、引用和代码阅读         | 服务端渲染正文；代码块提供语言标识、行号和复制操作        |
| `RelatedContent`     | 建立文章与项目/服务/联系的明确内部路径 | 转化事件只存文章 Slug、目标类型和目标 ID，不保存读者文本  |
| `ContentCalendar`    | 显示真实的已排期内容                   | 无排期时显示空状态，不虚构日历条目                        |
| `ContentAiAssistant` | 生成待审核的编辑草稿                   | 仅管理员可访问，草稿不自动保存或发布                      |

### 29.3 响应式、动效与可访问性

- 博客桌面端在搜索输入和分类控制之间保留清晰分组；移动端筛选条横向滚动而非压缩成不可点的小按钮。文章表格和代码块可以横向滚动，不强制缩小文本。
- 关联内容在宽屏使用自适应栅格，在窄屏自然单列；联系入口是可见链接，键盘焦点、标签状态与搜索结果数量都可被辅助技术读取。
- 内容工作台不使用滚动劫持或数字大屏动画；仅对输入、筛选按钮和卡片表面使用既有短过渡，`prefers-reduced-motion` 下直接稳定呈现。

---

## 30. 阶段二十国际化与全球化增量

### 30.1 语言与路由契约

- 首期仅支持 `zh-CN` 与 `en-US`：中文是默认语言且保持无前缀 URL，英文使用稳定的 `/en` 前缀；未来 `ja-JP`、`ko-KR` 只预留类型与配置扩展位，不提前渲染或加载语言包。
- 公开页面遵循同一镜像结构：`/projects` 对应 `/en/projects`、`/blog/[slug]` 对应 `/en/blog/[slug]`。语言切换必须保留当前路径、查询参数与页面锚点，不能把用户送回首页。
- 管理后台、API、静态文件和部署端点不参与语言重写；后台内容以明确的语言字段管理，绝不把后台管理动作误导到公开语言路径。

### 30.2 Language Switcher 组件契约

| 组件               | 职责                                   | 交互与无障碍约束                                                                    |
| ------------------ | -------------------------------------- | ----------------------------------------------------------------------------------- |
| `LanguageSwitcher` | 在当前公开页面的中文与英文版本之间切换 | 使用真实 button 或可访问链接；桌面 Header 与移动菜单均可到达；每个触控目标至少 44px |
| `LocaleLabel`      | 显示当前与可切换语言，例如 `中 / EN`   | 当前语言有文字、`aria-pressed` 或等价状态；不只依赖颜色                             |

- 它是紧凑的工具型控件，不做大号 CTA、国旗图标、下拉阴影、全页刷新动画或装饰性翻转。
- Hover 只使用现有 `surface-raised-hover`、文字颜色与焦点环；切换时只允许短 opacity / transform 反馈，`prefers-reduced-motion` 下立即稳定。
- 移动菜单将语言选择放在主导航之后、社交链接之前，采用清晰的 `Language` 分组。英文文案需写成自然的 `Chinese` / `English`，不能只留无解释的缩写。

### 30.3 本地化内容、SEO 与可访问性

- 静态文案来自按需加载的消息文件；项目、服务和文章以单一稳定 ID / slug 配合字段级语言内容，不复制成两套无法同步的数据列表。
- 英文首页、About、Services、Contact、Projects 与精选 Blog 必须使用人工维护的英文文案；未完成翻译的内容明确回退至可维护的默认内容，不以机器直译伪装为正式发布。
- 每个语言页面拥有自己的 `lang`、canonical、title、description、Open Graph 文案与站点地图 URL，并提供 `zh-CN`、`en-US` 和 `x-default` alternates / hreflang。
- 日期、阅读时长、表单标签、错误反馈、AI 助手引导与无数据状态都根据当前 locale 输出。屏幕阅读器只读取当前语言的一份内容。

### 30.4 性能与已接受边界

- 每次请求只加载当前 locale 的消息；语言切换器和公开链接使用国际化导航 API，不能在客户端加载另一套完整页面或内容数据。
- 保留现有服务端优先与动态导入策略；国际化不能让 Hero Canvas、Analytics、Assistant 或后台模块进入首屏额外关键路径。
- 首期英文 Blog 只发布经过人工整理的精选内容；新增语言、自动翻译工作流、地区域名、币种和 RTL 支持不属于本阶段。

---

## 31. 阶段二十三 CRM 与自动化获客增量

### 31.1 视觉角色与信息边界

- `/admin/crm/*` 延续后台工作台的深色、紧凑和安静密度；它是单人开发者处理真实合作机会的操作层，不伪装成大型 ERP 或销售大屏。
- 看板与列表只显示推进所需的信息：联系人、合作方向、来源、评分、状态与下一步；完整咨询内容仅在已鉴权的线索详情中显示。
- CRM 不使用虚构客户、模拟成交数据或自动播放图表。未连接数据库或无数据时，明确提示下一步。
- 敏感联系信息不得写入客户端分析事件、结构化日志或公开页面；邮件内容只由服务端事务发送。

### 31.2 新增令牌与组件契约

| Token                        | 用途                                   |
| ---------------------------- | -------------------------------------- |
| `--crm-kanban-column-min`    | 桌面看板单列的最小可读宽度             |
| `--crm-lead-card-min-height` | 线索卡片内容与触控控件的稳定最小高度   |
| `--crm-detail-grid-min`      | 线索详情双栏切换的最小可读宽度         |
| `--crm-workbench-transition` | 卡片、状态、筛选和抽屉的统一短反馈过渡 |

| 组件             | 职责                         | 约束                                                         |
| ---------------- | ---------------------------- | ------------------------------------------------------------ |
| `CrmNavigation`  | CRM 子路由的本地导航         | 真链接、有当前页状态；窄屏横向滚动，不能压缩为难点的小型标签 |
| `CrmLeadBoard`   | 线索分阶段看板               | 拖拽是增强功能；每张卡片保留 Select 阶段切换服务触摸与键盘   |
| `CrmStatusBadge` | 阶段、优先级、任务和项目状态 | 文字始终存在，不只用颜色；标签不承担主要操作                 |
| `CrmMetricGrid`  | 线索、转化、项目和来源汇总   | 只呈现真实聚合值；零数据保留清晰标签与空状态                 |
| `CrmDetailFacts` | 受保护的联系人与合作信息     | 使用语义化 Definition List；公开路由和分析事件绝不复用此数据 |

### 31.3 响应式、动效与可访问性

- 宽屏看板允许横向列组，窄屏保持横向可滚动列或普通顺序阅读；每张卡片的阶段 Select 保证拖拽不可用时依然能推进。
- 列、卡片、筛选和状态变化仅使用已有的表面、边界、opacity 与 transform 短反馈；不得为数据刷新加入装饰性数字跳动、旋转或连续动画。
- 任务、项目与自动化规则在手机端回退为单列；表格和行操作保留可见标签、键盘焦点、至少一个明确的保存动作和删除确认。
- `prefers-reduced-motion` 下不使用拖拽位移反馈或卡片升起，内容与 Select 控件直接稳定可用。

### 31.4 自动化、邮件与接受边界

- 首期自动化只有三条可开关规则：新线索确认邮件、管理员通知和 24 小时后的内部跟进任务；不发送循环催促、批量营销或没有人工上下文的邮件。
- Resend 发件只在 `RESEND_API_KEY`、`CRM_EMAIL_FROM` 与对应接收邮箱配置完成时运行；未配置时 CRM 仍创建线索，但后台明确提示邮件未启用。
- 客户项目状态邮件是事务性进度通知，不包含预算、咨询正文或其他线索的私密资料。
- AI Assistant 保持会话仅在浏览器 sessionStorage；它只提供受限的项目意向分类和 Contact 引导，绝不自动把对话写入 Lead。

---

## 32. 阶段二十四 AI Business Copilot 增量

### 32.1 视觉角色与信息边界

- `/admin/ai/*` 是管理员的 AI 工作台，不是面向访客的聊天页面，也不是实时成本大屏。它沿用后台的紧凑深色表面、细边界与真实空状态。
- 工具入口按“线索分析、方案草稿、内容草稿、会议总结、项目计划、知识草稿”排列。每个入口在文案上明确为“生成待审核草稿”，不能用“自动执行”“自动发布”或“自动成交”表述。
- 用量日志只显示代理类型、成功状态、时间、模型与 Token 数；不显示客户正文、会议记录、提示词、密钥或完整模型回复。

### 32.2 新增令牌与组件契约

| Token                               | 用途                               |
| ----------------------------------- | ---------------------------------- |
| `--ai-center-tool-min-height`       | 宽屏工具入口保持可读的最小块高     |
| `--ai-center-result-max-block-size` | 草稿与计划预览的受控最大高度       |
| `--ai-center-transition`            | 工具卡、日志和状态表面的统一短反馈 |

| 组件                 | 职责                                 | 约束                                                          |
| -------------------- | ------------------------------------ | ------------------------------------------------------------- |
| `AiCenterNavigation` | AI 概览、日志、Prompt 与设置的子导航 | 真链接、当前页状态；窄屏横向滚动，不压缩为难点的小型标签      |
| `AiWorkflowTools`    | 各 Agent 的明确人工触发入口          | 使用真实 Label 与表单；提交后必须回到可见反馈                 |
| `AiUsageLogList`     | 只读、脱敏的调用用量记录             | 不渲染提示词、输入、输出、客户信息或密钥                      |
| `AiDraftReviewList`  | 内容、项目和知识的待审核草稿提示     | 明确显示草稿/待确认状态；只有人工确认才创建项目任务或启用知识 |
| `AiPromptManager`    | 版本化管理管理员指令                 | 仅受保护后台；基础安全规则始终由服务端保留                    |

### 32.3 响应式、动效与可访问性

- 宽屏以两列工作台组织工具、草稿与日志；`<1024px` 回退为单列文档流。工具表单中的 Select、Textarea 与按钮均沿用后台控件高度和焦点环。
- 工具卡只在 Hover/Focus 时提升边界和表面；状态更新使用既有短 opacity/transform 反馈。不得让模型生成、Token 刷新或日志列表带来数字跳动、自动滚动或装饰性动画。
- 日志和状态永远同时提供文字，不只依赖颜色；草稿计划的“确认并创建任务”是独立按钮，键盘和触摸均可使用。reduced motion 下直接显示稳定界面。

### 32.4 安全与已接受边界

- 模型密钥继续只由服务端环境变量读取。数据库模型配置只管理 provider、model、优先级与用量上限，绝不保存 API Key。
- AI 代理只将可审核结果写入受保护的 Lead、Proposal、MeetingNote、Content Draft、Project Plan 或 disabled Knowledge；模型失败、预算超限和注入输入均转人工处理，不阻塞 CRM 线索持久化。
- 首期不实现自动报价、自动发送方案、自动发布内容、自动签约、自动创建向量、微信/Telegram 实际投递或跨平台社交发布。通知渠道只保留配置与后续适配边界。

---

## 33. 阶段二十五 SaaS 协作空间增量

### 33.1 产品界面角色

- `/client` 与 `/dashboard/projects/[id]` 是企业客户与协作成员的交付空间，不复用公开营销页的超大叙事，也不伪装成复杂 ERP。
- 视觉采用现有深色画布、细边界、紧凑数据密度和受控的紫色行动色；项目进度、任务、文档、时间线与 AI 助手都有明确文字标签，不能用颜色或 Hover 作为唯一信息来源。
- `/admin/saas` 是管理员的全局 SaaS 运营入口，只呈现真实组织、成员、套餐、订阅与审计数据；数据库未配置或没有数据时保持清晰的配置提示或空状态。

### 33.2 新令牌与组件契约

| Token                         | 用途                                   |
| ----------------------------- | -------------------------------------- |
| `--saas-sidebar-width`        | 客户门户与项目协作空间的宽屏导航宽度   |
| `--saas-control-height`       | 任务、文件和聊天控件的稳定最小触控高度 |
| `--saas-kanban-column-min`    | 桌面 Kanban 列保持可阅读的最小宽度     |
| `--saas-panel-gap`            | 概览、任务、文档和动态面板的统一间距   |
| `--saas-workspace-transition` | 任务状态、面板和反馈的统一短交互过渡   |
| `--saas-ai-result-max-block`  | 项目助手输出的安全可读高度             |

| 组件                 | 职责                                                | 约束                                                          |
| -------------------- | --------------------------------------------------- | ------------------------------------------------------------- |
| `ClientPortalHeader` | 当前企业、项目入口与退出操作                        | 当前组织有文本身份；小屏不压缩成图标谜题                      |
| `ProjectWorkspace`   | Overview、Tasks、Documents、Timeline、AI 的阅读顺序 | 服务端输出真实内容；缺失数据使用诚实空状态                    |
| `ProjectTaskBoard`   | 四列 Todo / Doing / Review / Done 看板              | Select 是核心状态切换；不依赖拖拽；移动端横向可滚动或自然单列 |
| `ProjectFilePanel`   | 私有文件和 Markdown 文档入口                        | 不暴露公开 Blob URL；下载与上传始终通过组织权限 API           |
| `ProjectAssistant`   | 项目知识库问答                                      | 仅在用户主动发送后请求；不显示其他组织资料、提示词或模型密钥  |
| `SaasAdminDashboard` | 组织、成员、套餐、订阅与审计概览                    | 仅管理员可进入；数据没有配置时不显示虚构指标                  |

### 33.3 响应式、动效与无障碍

- `≥1024px` 可使用固定或 Sticky 的窄侧栏；小于该断点时改为顶部项目导航和单列内容。任务列在窄屏可以横向滚动，但每列仍有清晰标题与正常键盘顺序。
- 任务状态、上传反馈与 AI 回复只使用现有的 opacity、transform、边界和表面短过渡；不得添加连续数字跳动、自动滚动、卡片翻转或装饰性发光。
- 每个任务状态、项目进度、文件类型和权限状态都有文字；上传、下载、创建任务和发送 AI 问题均保留可见 label、焦点环、错误反馈和至少 44px 触控区域。
- `prefers-reduced-motion` 下 Kanban、面板、上传反馈和 AI 窗口直接稳定显示。客户门户不得加载 Hero Canvas、营销动画或公开站的悬浮助手。

### 33.4 数据隔离与接受边界

- UI 不以隐藏菜单作为授权手段。每次读取、任务更新、文件下载、文件上传和项目 AI 请求都在服务端带 `organizationId` 与成员关系校验。
- 项目文件采用私有存储路径；即使知道文件 ID 或路径，也必须通过被授权的组织 API 才能下载。当前首期仅实现 Vercel Blob 私有路径；S3 与 Cloudflare R2 是同一存储接口的后续适配器。
- 项目 Markdown 文档会进入对应 `Organization + Workspace` 的知识命名空间；向量表预留 pgvector 字段和组织过滤，Embedding 生成任务不在本期自动执行。
- 套餐、订阅、账单和支付模型只建立数据边界，不连接 Stripe、Paddle 或任何真实扣费流程；实时聊天、WebSocket 通知和完整 Notion 级协同编辑留给后续阶段。

---

## 34. 阶段二十六 SaaS 商业化增量

### 34.1 产品界面角色与信息边界

- `/pricing` 是帮助访客理解套餐价值的决策页：四个套餐使用稳定、易比较的阅读顺序，不做倒计时、虚假折扣、伪造客户数量或强迫升级弹窗。
- `/dashboard/settings/billing` 是已登录组织的账单操作页：只展示当前套餐、真实用量、试用或续费时间及服务端允许的升级/取消动作；前端从不写入订阅状态或价格。
- `/admin/business` 与 `/admin/billing` 是运营工作台：沿用现有后台的紧凑深色表面，收入、订阅、用量和流失指标只读自真实聚合数据；没有数据库或没有记录时使用明确配置/空状态，不填充演示数字。
- 企业版是人工沟通入口，首期不在页面中承诺 SSO、私有部署、SLA、银行卡处理或国内支付渠道已经可用。

### 34.2 新令牌与组件契约

| Token                          | 用途                                       |
| ------------------------------ | ------------------------------------------ |
| `--billing-plan-grid-min`      | 套餐网格单列的最小可读宽度                 |
| `--billing-summary-panel-min`  | 当前套餐与用量面板的稳定最小高度           |
| `--billing-usage-track-height` | 用量进度条的固定高度                       |
| `--billing-metric-grid-min`    | 商业运营指标卡的最小可读宽度               |
| `--billing-transition`         | 套餐卡、用量面板与账单动作的统一短反馈过渡 |

| 组件                 | 职责                               | 约束                                                                 |
| -------------------- | ---------------------------------- | -------------------------------------------------------------------- |
| `PricingPlanGrid`    | 显示公开套餐、价值重点、限制与入口 | 关键价格与限制不依赖 Hover；每张卡片有完整标题与行动文字             |
| `BillingOverview`    | 当前套餐、订阅状态、时间与使用量   | Client 叶组件仅发起受保护的订阅动作；不接收或保存支付密钥            |
| `BillingUsageMeter`  | 显示用量、上限、无限状态与升级提示 | 始终同时给出文字和数值；进度不只依赖颜色                             |
| `BusinessMetrics`    | MRR、ARR、订阅与转化等只读汇总     | 零值必须带时间范围或无数据说明；不得用动画夸大真实业务变化           |
| `BillingAdminTables` | 套餐、订阅、支付的只读运营列表     | 窄屏可横向滚动；金额、状态和时间均有文本，不把敏感支付信息带到客户端 |

### 34.3 布局、交互与自适应

- 公开 Pricing 在宽屏用自适应网格呈现四档套餐，Pro 可以以细边界和“适合个人开发者”文字作为轻量推荐；移动端自然单列，按钮始终至少 `--form-control-height`，不压缩价格或限制说明。
- 账单页在宽屏把“当前订阅”和“用量”并列，窄屏按当前套餐 → 操作 → 用量 → 说明的文档顺序堆叠；升级和取消拥有明确加载、成功与错误反馈，失败后不清空已有账单信息。
- 后台商业指标在宽屏使用自适应指标网格和可横向滚动的表格容器，在 `<1024px` 回退为单列；表格不引入大屏数字跳动、图表库或自动刷新动画。
- Hover 只提升表面或边界，动作反馈仅使用现有 `opacity`、`transform` 与 `--billing-transition`。`prefers-reduced-motion` 下立即稳定，不能让支付、取消或升级结果依赖动画理解。

### 34.4 安全、可访问性与接受边界

- 套餐金额以最小货币单位在服务端保存；浏览器只能提交受 Zod 限制的套餐 slug 或取消意图，Checkout 价格、订阅状态和权限均由服务端与 Stripe Webhook 决定。
- Webhook 只接收原始请求体并验证 Stripe Signature；重复事件通过 provider event ID 幂等记录。页面、客户端脚本和公开 API 永远不读取 Stripe Secret、Webhook Secret、客户支付信息或第三方交易原始载荷。
- 套餐限制与功能开关统一经过 `src/lib/permissions` 和服务端 billing 层；项目、AI、私有知识与存储入口不能靠隐藏菜单或 URL 参数绕过。
- 当前接受边界：实际 Stripe Checkout、Webhook 与生命周期邮件需要生产数据库和环境变量完成后才会启用；未配置时页面与 API 只能给出诚实的配置提示，不能模拟支付成功。微信支付、支付宝、SSO、私有部署和 SLA 仅保留后续适配边界。

---

## 35. 阶段二十七 AI SaaS Platform 增量

### 35.1 产品界面角色与信息边界

- `/dashboard/ai` 是组织成员创建、阅读和管理企业 AI 能力的紧凑工作台；它沿用客户门户的深色画布、细边界和文档流，不套用公开品牌站的沉浸式叙事。
- `/dashboard/ai/[assistantId]` 是单一助手的专注问答界面：标题、会话、来源和输入保持固定阅读顺序。消息不自动持久化，页面不展示系统提示词、密钥、其他组织资料或完整原始日志。
- `/admin/ai-platform` 是管理员的聚合运营页，只展示 Workspace、模型、文档作业、Token、成本和失败状态；不能显示企业用户的完整对话或文档正文。
- 状态文本始终明确：`可检索`、`处理中`、`失败`、`已撤销` 不只依赖颜色。API Key 仅在签发瞬间显示一次，之后只显示非敏感前缀。

### 35.2 组件契约

| 组件                      | 职责                                             | 约束                                                                         |
| ------------------------- | ------------------------------------------------ | ---------------------------------------------------------------------------- |
| `AiPlatformDashboard`     | Workspace、助手、知识库、用量和 API 的组织级概览 | 仅管理角色显示创建与 Key 操作；普通成员仍只看到授权后的资料与助手            |
| `AiAssistantChat`         | SSE 流式问答、消息与 Sources 呈现                | 来源在回答完成后以文档名和片段序号显示；聊天内容只保存在当前浏览器组件状态   |
| `AiDocumentForms`         | 文本与文件资料的异步提交入口                     | 提交后显示处理反馈，不把上传和解析伪装成即时完成；文件状态来自服务端任务记录 |
| `AiPlatformAdminOverview` | 运营聚合、模型和失败任务                         | 使用真实数据库聚合；没有配置或数据时显示明确空状态，不生成演示指标           |

### 35.3 布局、状态与响应式

- 桌面端以两列组织资料列表/导入表单和 API/Key 区域；助手卡片与指标卡在宽屏可形成二至四列，窄屏一律回退为自然单列。
- Chat 消息最大宽度受控，用户消息靠右、助手消息靠左，但每条消息都保留文字角色标签。Sources 在输入框之前，保证键盘和屏幕阅读器遵循“回答 → 依据 → 继续提问”。
- 仅使用既有 `surface`、`border`、`brand`、`cyan`、状态色、圆角和短反馈令牌。不会引入渐变文字、大屏数字跳动、连续粒子、聊天自动滚动或装饰性发光。
- `prefers-reduced-motion` 下所有工作台表面、状态与发送按钮直接稳定；AI 输出的逐段到达是内容传输，不作为必须理解的动效。

### 35.4 安全与接受边界

- 组织、AI Workspace、助手、向量分片、文档、处理作业、用量和 API Key 都在服务端使用 `organizationId`，检索同时约束 `workspaceId`；角色受限文档还会过滤成员角色。
- 文档处理先写入持久化 Job 与状态，再由受保护的处理接口执行解析和分块。首期不引入 Redis/BullMQ，因此队列是数据库持久化作业而非独立 worker；生产大文件吞吐可在后续替换为专用队列消费者。
- `pgvector` 字段已作为向量数据域预留。没有配置 Embedding Provider 时，系统以同一组织与 Workspace 边界内的词法检索回退，不伪造语义向量结果；启用 Embedding Provider 后可填充该字段并接入向量相似度查询。
- 所有模型密钥只从服务端环境变量读取；API Key 只存哈希；日志只记录模型、时间、成功状态、Token 与成本字段。模型、支付和外部 API 未配置时，页面与接口返回真实的不可用状态，不能模拟成功回答。

---

## 36. 阶段二十八 AI Developer Marketplace 增量

### 36.1 页面角色与公开边界

- `/marketplace` 是公开、搜索优先的产品目录，只展示已通过人工审核的发布快照。它不是客户门户，也不读取组织的私有 Assistant、文档、API Key 或用量。
- `/developer` 与 `/developers/docs` 是公开开发者入口：前者说明 API 接入价值与安全边界，后者提供认证、请求、响应和 SDK 预留文档。示例只能使用占位 ID 和脱敏 Key 前缀。
- `/dashboard/ecosystem` 是组织成员的创作者工作台，展示其真实发布、收藏、调用和收益预留状态；数据库没有数据时使用清楚的空状态，不能填充虚构成交数。
- `/admin/marketplace` 是人工审核与下架入口。AI 风险检查只显示为“辅助检查”状态，发布、拒绝和归档永远要求管理员动作。

### 36.2 组件契约

| 组件                     | 职责                                                   | 约束                                                                                       |
| ------------------------ | ------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| `MarketplaceExplorer`    | 搜索、类型/分类筛选、分页后的内容扫描                  | 搜索输入延迟执行；关键标题、作者、价格和审核状态不依赖 Hover 才可读取                      |
| `MarketplaceItemCard`    | 单个公开条目摘要                                       | 使用语义化 `article`；只输出审核后快照字段；价格未知时显示“免费”或“暂未定价”，不伪造成交量 |
| `MarketplacePublishForm` | 创作者提交 Agent、Prompt、Workflow、Template 或 Plugin | 明确草稿/待审核状态；不允许从浏览器提交密钥、私有文档内容或未声明权限                      |
| `DeveloperApiDocs`       | Developer API 认证、示例与安全限制                     | 示例 Key 永远使用固定掩码；代码块可复制但不包含真实凭据                                    |
| `EcosystemDashboard`     | 创作者的发布、使用、收藏、收益预留概览                 | 指标来自服务端聚合；缺少收入流水时显示“尚无结算记录”                                       |
| `MarketplaceReviewQueue` | 管理员审核、批准、拒绝和归档                           | 状态文本与图标并存；危险动作必须走显式表单提交，不做隐式切换                               |

### 36.3 布局、响应式与交互

- Marketplace 桌面端优先采用“搜索与过滤条 + 两至三列内容目录”；平板两列；手机单列并将筛选收束为可展开区域。公开条目使用已有 `surface`、`border`、状态和排版令牌，不新增渐变标题或营销式统计墙。
- Developer Docs 使用受控宽度的阅读列与代码示例区；小屏按“说明 → 端点 → 请求 → 响应 → 限制”自然垂直排列，不能依赖横向滚动阅读正文。
- Dashboard 和审核台沿用 SaaS 客户门户/后台的紧凑信息密度、细边框和真实空状态。所有交互使用现有 focus 环、按钮状态及 `prefers-reduced-motion` 回退。

### 36.4 安全、内容与接受边界

- 公开条目保存版本化 Manifest 快照，而不是直接暴露私有 `AiAssistant`、知识库或组织资源；发布操作始终从服务器取得当前用户和组织，绝不信任请求中的 creator/user ID。
- Plugin 首期是声明式 Manifest 和权限白名单：`read_document`、`call_api`、`access_database` 等权限必须明确列出；不执行第三方任意代码，也不授予无限权限。
- Developer API 复用哈希化、可撤销 Key，按组织权益和限额计量；公开 Agent Chat 仅允许已发布、启用且安全审核通过的 Agent 快照。请求被限流或模型未配置时返回真实错误，不能模拟回答。
- Marketplace 收益、订单和分账只建立独立数据边界与状态入口；首期不复用 SaaS Subscription 或 Stripe 订阅 Webhook 处理单次商品支付，未配置支付时不展示“已购买”假状态。

---

## 37. 阶段二十九企业级能力增量

### 37.1 页面角色与信任边界

- `/dashboard/security` 是企业成员的安全中心：只呈现当前企业的策略摘要、已验证域名、SSO 就绪度、可见审计摘要和 AI 内容安全状态；它不是安全扫描器的营销展示，也不暴露密钥、会话或原始 IP。
- `/admin/enterprise` 是平台管理员的企业运营面板：组织、部门、成员角色、SSO 配置状态和 API Gateway 风险只从真实数据库聚合；没有数据库记录时显示明确空状态，而非虚构企业指标。
- 企业结构固定为 `Enterprise → Organization → Department → Workspace → Project → Resource`。每个详情或动作均在服务端以 enterprise、organization 和资源范围复核，界面上的隐藏或禁用绝不能被视作授权。

### 37.2 新令牌与组件契约

| Token                               | 用途                                  |
| ----------------------------------- | ------------------------------------- |
| `--enterprise-security-grid-min`    | 安全摘要卡在宽屏保持可读的最小宽度    |
| `--enterprise-audit-row-min-height` | 审计表项稳定的最小点击/阅读高度       |
| `--enterprise-risk-panel-min`       | 风险、SSO 与 API Key 卡的受控最小高度 |
| `--enterprise-surface-transition`   | 安全卡、状态徽标和表格行的统一短反馈  |

| 组件                        | 职责                                       | 约束                                                         |
| --------------------------- | ------------------------------------------ | ------------------------------------------------------------ |
| `EnterpriseSecurityCenter`  | 安全策略、SSO、域名、审计和 AI 风险概览    | 文字说明每个状态；不输出密钥、未脱敏网络信息或完整文档正文   |
| `EnterpriseSecurityActions` | 域名添加、DNS 令牌复制、SSO 配置和审计导出 | 仅安全/企业管理员可见；危险配置和导出使用明确按钮与结果反馈  |
| `EnterpriseAdminOverview`   | 平台管理员的企业、组织、部门和安全状态列表 | 使用真实聚合与空状态；窄屏保持可横向阅读的表格语义           |
| `EnterpriseGatewayPanel`    | API Key scope、有效期、轮换与使用边界      | Key 只在签发或轮换瞬间展示一次；列表只显示前缀、scope 和状态 |

### 37.3 布局、动效与可访问性

- 宽屏安全中心采用“安全摘要卡 + 主要审计列”的两列文档流；`<1024px` 一律变为单列，审计和组织表格放入明确标记的横向滚动容器，不能截断关键动作或状态。
- 状态仅使用既有深色表面、细边界、紫色行动色及成功/警告/危险的文字加图标组合。Hover 只提升边界和表面；不使用连续扫描、雷达、闪烁告警或会让风险等级难以阅读的装饰动画。
- 所有安全状态都有语义文字（如“已验证”“待验证”“需人工复核”“已撤销”）；表单有可见 Label，审计导出和 DNS 令牌复制可经键盘完成。`prefers-reduced-motion` 下立即呈现稳定状态。

### 37.4 已接受的安全与交付边界

- SSO 首期保存并验证 SAML/OIDC/OAuth2 配置边界、域名归属与发现信息；真实跳转登录需要客户提供 IdP metadata、client secret、证书和回调地址，未配置时页面明确显示“未启用”，绝不伪造登录成功。
- API Key 始终只存哈希，最小 scope、过期时间、撤销和轮换均在服务端执行。固定窗口限流是当前单实例保护；跨实例的分布式限流可在后续接入 Redis。
- AI 文档在解析前执行基础密钥、私钥、注入与敏感身份证号扫描。阻断内容不进入检索；待复核内容保持 `SECURITY_REVIEW`，需企业管理员确认后才可处理。
- 私有化部署、备份、导出、Docker Compose 与数据库迁移提供可执行配置和文档；生产 SSO、Redis、对象存储、WAF、KMS、合规认证和灾备演练仍需要真实基础设施与企业环境变量，不能由前端页面宣称已经完成。

---

## 38. 阶段三十 AI 企业操作系统增量

### 38.1 工作台角色与信息边界

- `/dashboard/ai-workspace` 是组织成员主动发起、阅读和跟进 AI 任务的工作台。它不是营销页面，也不把 Agent、工具或知识数据包装成未经验证的“自动员工”。
- `/dashboard/ai-observability` 只呈现当前组织的运行摘要、任务状态、审批和脱敏 Trace；不展示原始提示词、API Key、其他组织的资料或完整知识正文。
- `/admin/ai-governance` 是平台管理员的全局治理视图。模型、Agent、工具、审批与 Trace 均使用真实数据库聚合；无数据库或无数据时显示明确的配置/空状态。
- 工作台的关键路径是：选择 Workspace → 描述任务 → 获得可复核报告，或进入人工审批 → 在 Trace 中复盘。高风险业务动作不能由页面暗示为已经执行。

### 38.2 新增令牌与组件契约

| Token                          | 用途                                       |
| ------------------------------ | ------------------------------------------ |
| `--aios-metric-grid-min`       | 运行指标卡的最小可读宽度                   |
| `--aios-canvas-min-height`     | Workflow 只读流程画布的稳定最小高度        |
| `--aios-row-min-height`        | Agent、审批、报告与 Trace 行的稳定阅读高度 |
| `--aios-trace-max-block-size`  | Trace 和报告摘要的安全最大阅读高度         |
| `--aios-task-input-min-height` | 任务文本输入的最小可读高度                 |
| `--aios-transition`            | 任务提交、审批与状态表面的统一短反馈过渡   |

| 组件                         | 职责                                        | 约束                                                             |
| ---------------------------- | ------------------------------------------- | ---------------------------------------------------------------- |
| `AiOperatingSystemDashboard` | Agent、Workflow、Knowledge、Reports 的总览  | Server 输出真实概览；没有数据时提供明确创建入口                  |
| `AiTaskComposer`             | 选择 Workspace、提交受限任务与显示结果      | Client 叶组件；请求通过受保护 API；文本域有显式 Label 和错误反馈 |
| `AiWorkflowCanvas`           | 只读的 Trigger → Agent → Tool → Action 流程 | 首期不提供拖拽编辑；节点有文字标签，移动端改为自然纵向阅读       |
| `AiApprovalQueue`            | 高风险任务的批准或拒绝入口                  | 仅 `ai.manage` 成员可操作；按钮明确说明不会直接执行外部业务写入  |
| `AiObservabilityDashboard`   | 运行、用量、审批、报告与 Trace 的组织级视图 | 不渲染提示词、原文档、密钥或其他租户数据                         |
| `AiGovernanceDashboard`      | 模型、Agent、工具和治理策略的管理员聚合     | 只读真实状态；配置能力留给既有 AI Platform 和后续受保护动作      |

### 38.3 布局、状态与响应式

- 宽屏工作台以“运行状态与提交面板”及“Agent/Workflow”两层阅读结构组织；指标最多四列，详情最多两列。小于 `1024px` 时统一回退为单列，流程画布按节点顺序折叠而不是缩放成难读小图。
- 任务输入、审批、Agent、工具、报告和 Trace 都使用现有深色表面、细边界、14px 卡片圆角与受控紫色行动色。成功、等待、失败和已批准均以文字加图标同时表达。
- 运行中的视觉反馈只允许边界、表面、opacity 与 transform 的短过渡；没有数字跳动、自动滚动、扫描光、连续闪烁或装饰性粒子。
- `prefers-reduced-motion` 下任务结果、审批状态和 Workflow 节点立即稳定显示；内容不依赖动画、Hover 或拖拽才可理解。

### 38.4 安全与接受边界

- 所有 AIOS 查询和动作均通过 Enterprise、Organization 与 Workspace 作用域；页面隐藏操作不是授权，服务端仍执行 RBAC、套餐权益、治理策略和输入边界检查。
- 高风险项目任务会先生成审批记录。审批通过仅记录批准状态；未配置企业工具适配器时，不执行邮件、CRM、项目或数据库写入。
- 首期队列使用数据库中的 `AiTaskRun`、`AiApprovalRequest` 与 `AiTaskTrace` 作为持久化执行状态。Redis/BullMQ、实时流式长任务、React Flow 拖拽编辑器和外部业务工具执行器是后续可替换的适配层，页面不得暗示它们已启用。

---

## 39. 阶段三十一 AI 原生应用生态增量

### 39.1 产品角色与关键路径

- `/dashboard/apps` 是组织内 AI 应用的生命周期入口：先选模板或新建，再进入 Builder，完成测试后才可发布。它不复刻公开 Developer Marketplace；公开 Marketplace 继续承载跨组织的审核发布包，而这里是当前组织的内部应用目录。
- `/dashboard/apps/builder` 是“三栏、单任务”工作台：左侧是可拖拽的 Block 库，中间是可连接的工作流画布与应用预览，右侧是当前应用、模型、Workspace 与访问范围设置。每个拖拽操作都有“添加区块”的键盘/触摸替代入口。
- `/app-marketplace` 只显示当前组织内已发布、且当前成员匹配部门/角色/成员访问规则的应用。`/app/[slug]` 是员工实际使用的受保护运行页，不是面向互联网公开的聊天链接。
- 生命周期固定为 `Draft → Testing → Published → Archived`：Testing 允许管理角色在 Sandbox 中调用真实受控聊天链路；Published 才允许满足访问规则的成员使用；Archived 会立即停止助手与工作流。工具 Block 仅声明连接意图，未配置工具适配器时绝不执行邮件、CRM、数据库或任意第三方写入。

### 39.2 新增令牌与组件契约

| Token                             | 用途                                         |
| --------------------------------- | -------------------------------------------- |
| `--ai-app-builder-min-height`     | 宽屏 Builder 三栏稳定的最小工作区高度        |
| `--ai-app-builder-palette-inline` | 左右配置栏在宽屏的可读基准宽度               |
| `--ai-app-canvas-node-inline`     | React Flow 节点的最小可读宽度                |
| `--ai-app-preview-min-height`     | 中央应用预览的稳定最小高度                   |
| `--ai-app-market-grid-min`        | 内部应用市场与列表条目的最小可读宽度         |
| `--ai-app-chat-max-block-size`    | Sandbox 与已发布应用回答区的安全最大阅读高度 |
| `--ai-app-transition`             | 区块、状态、边界和抽屉的统一短反馈           |

| 组件                       | 职责                                     | 渲染与无障碍约束                                                                 |
| -------------------------- | ---------------------------------------- | -------------------------------------------------------------------------------- |
| `AiAppsDashboard`          | 应用清单、模板入口、生命周期状态与空状态 | Server 数据进入 Client 叶组件；无数据库时显示真实配置提示                        |
| `AiAppBuilder`             | Block 配置、画布、预览、保存/测试/发布   | Client 叶组件；拖拽可用但不依赖拖拽；按钮、输入和状态反馈均有可访问名称          |
| `AiAppWorkflowCanvas`      | Input/Agent/Knowledge/Tool/Output 节点图 | React Flow 按需进入 Builder 路由；节点文字不被颜色单独表达；小屏退为纵向操作区   |
| `AiAppSandbox`             | Testing 环境下的真实模型问答与来源输出   | 只允许管理角色；SSE 回答有 loading/error 反馈；不渲染提示词、密钥或原始私有资料  |
| `AiInternalAppMarketplace` | 组织内发布应用目录                       | 只显示当前成员已授权应用；无 Hover 依赖；每项清楚说明状态与使用入口              |
| `AiAppChat`                | 已发布应用的员工使用面                   | 真实表单、显式 label、来源列表与可恢复错误；不允许跨组织或测试中应用绕过访问规则 |

### 39.3 布局、响应式与动效

- `≥1280px` 的 Builder 使用约 `1fr minmax(0, 2.1fr) 1fr` 的三栏结构，中间画布始终保留最小可读高度；`768px–1279px` 变为“画布主列 + 配置副列”；`<768px` 依次展示应用摘要、Block 添加器、工作流节点列表、预览和设置，不把画布缩小成不可操作的迷你图。
- Node 以产品层级表达流程，使用现有 surface、border、brand/cyan 和状态令牌；`Background` 只服务实际编辑画布的定位网格，不作为页面装饰背景。外层不增加玻璃、扫描线、营销统计墙或多层卡片嵌套。
- Drag、边连接、选择和保存仅使用短 `transform/opacity/border-color` 反馈。状态变更有文字与图标；动画不阻塞发布、返回或键盘操作。reduced motion 下画布和预览保持立即稳定。
- 内部市场宽屏使用两至三列目录，平板两列、手机单列；表单与按钮遵守既有 `--saas-control-height`，避免仅 Hover 才可见的关键动作，长名称和状态允许自然换行。

### 39.4 数据、权限与接受边界

- `AiApp`、`AiAppVersion`、`AiAppTemplate`、`AiAppAccessRule` 和 `AiWorkflow.edges` 都持久化在 Prisma，并在每次操作中以 enterprise、organization、workspace 作用域复核。模板可从首期内置目录初始化，但应用实例、版本和访问规则始终属于真实组织数据。
- 运行时复用现有 `AiAssistant` 与 RAG/模型/计量链路；应用配置只保存在服务端数据库，模型密钥继续只从服务端环境读取。模型未配置、资料不足或流中断时显示真实错误或限制说明，不伪造回答。
- Testing 与 Published 的 API 分离：Sandbox 只对 `ai.manage` 成员开放；发布应用的对话 API 在服务端验证 Published 状态与当前角色/部门/成员规则。API 响应和 Trace 不返回系统提示词、访问令牌、密钥或其他组织数据。
- 首期不提供多人协同编辑、版本回滚界面、跨组织安装、付费购买、外部工具执行器、可视化审批节点或生产队列。React Flow 只编辑声明式工作流 JSON；真正的高风险动作仍由阶段三十的审批与工具适配器边界控制。
